{
  "$comment": [
    "Dos de los doce guards. El resto queda afuera por lo que dice AGENTS.md, «Acá no se instala nada».",
    "",
    "`git-add` es una regex sobre el comando y no cuesta nada. `verify` corre la suite (`npm test`, no",
    "`npm run ci`: quedan afuera check, automatización, integraciones y cobertura) y sólo si hay código",
    "staged, así que un commit de documentación no paga los ocho segundos. Para saltarlo con intención:",
    "OPS_SKIP_VERIFY=1."
  ],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/automatization/hooks/guard-git-add.sh"
          },
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/automatization/hooks/guard-verify.sh"
          }
        ]
      }
    ]
  }
}
