# Staging Jira

Directorio administrado por `integration sync`. No edites `remote.json` ni `sync-state.json`; cura solamente
`draft.md`. Las categorías son `epics/`, `stories/`, `tasks/`, `subtasks/` e `items/`.
