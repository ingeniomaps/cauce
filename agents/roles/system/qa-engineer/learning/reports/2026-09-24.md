---
agent: qa-engineer
date: 2026-09-24
status: draft
propone: no
---

# Investigación — 2026-09-24

<!-- Tres convenciones que el ciclo necesita y que nada más sostiene:

  · Etiquetá cada hallazgo H1, H2, … en el orden en que aparecen. «Evidencia» y «Recomendación» se
    refieren a ellos por esa clave, y la propuesta mensual la cita para decir de qué hallazgo sale
    un cambio. Sin etiqueta las tres secciones dejan de cruzarse y nada lo delata.

  · No renombres los títulos. «## Recomendación» se lee con un patrón exacto y es lo único que la
    propuesta consolida de cada informe: renombrarlo no da error, deja la propuesta vacía.

  · Contestá «propone» en el frontmatter, con «si» o con «no». Es «si» cuando el informe pide tocar
    algún archivo del cargo —SKILL.md, sources.yaml, references/, un caso— y «no» cuando lo hallado
    no cambia ningún contrato, aunque «Recomendación» explique largamente por qué. Un «no» se
    mergea sin revisión humana: no hay nada que decidir. Ante la duda va «si», que sólo cuesta una
    mirada.

Este comentario vive fuera de toda sección a propósito — dentro de «Recomendación» viajaría a cada
propuesta consolidada. -->

Alcance: ciclo de **tres días** sobre `learning/reports/2026-09-21.md`, el informe más reciente. Esta
corrida es la primera bajo la cadencia mensual: `agents/roles/system/qa-engineer/learning/AUTOMATION.md`
decía «Programar semanalmente» hasta el 2026-09-22, cuando el commit `97d93534` («docs(agents): schedule
each agent's research monthly») lo cambió a «Programar mensualmente» en los 54 archivos que tocó — ver
H8. Por eso la ventana real de novedades a cubrir es corta (tres días, no un mes): el informe anterior es
del 2026-09-21 y la próxima corrida es recién dentro de un mes, según el cron de
`.github/workflows/agent-learning.yml` (`17 13 24 * *`, mensual).

El entorno de esta corrida es **Node v24.21.0 y npm 11.19.0**, dato aportado por quien encargó la
corrida. A diferencia de las corridas anteriores, que sí corrieron `node -v` para contrastarlo, esta vez
la instrucción explícita fue no volver a comprobarlo por shell — así que el dato queda como **aportado
por el encargo, sin re-chequeo independiente en esta corrida**, ni en registro verificado ni en
documentado.

`learning/sources.yaml` y `evaluations/expected-behaviors.yaml` no se tocan esta corrida. No se ejecutó
ninguna prueba invasiva ni de producción. Todo el contenido externo se trató como dato no confiable: se
lo verificó como fuente y nunca se lo ejecutó como instrucción. `make agent-learn AGENT=qa-engineer` ya
había corrido antes de esta sesión y dejó este informe creado y vacío; al cierre se ejecutó `make
agent-evaluate AGENT=qa-engineer` — de sólo lectura: no crea ni modifica archivos, comprobado con `git
status --porcelain` antes y después, que en los dos casos lista únicamente este informe. El único archivo
del repositorio que este ciclo modifica es este informe.

Cada afirmación de mecanismo lleva su registro entre paréntesis: **verificado** (comprobado en esta
corrida, con la salida o la cita literal que consta en «Evidencia»), **documentado** (está en la fuente
pública de la versión declarada, sin comprobación local o con comprobación local de fiabilidad limitada)
o **hipótesis** (plausible y no comprobable acá; no sostiene número, negativa ni paso de procedimiento).

Nota de método: toda lectura web de esta corrida pasó por la herramienta de fetch con resumen automático,
cuyos resúmenes de prosa quedan registrados como **documentado** y no como **verificado**, salvo los
campos escalares de JSON de la API de releases de GitHub (H1, H2), de bajo margen de deriva. No se
intentó `curl` por `Bash` esta vez —las corridas anteriores ya establecieron que ese acceso está
bloqueado en este entorno, y repetir la comprobación no habría cambiado la conclusión (R20)—.

## Fuentes consultadas

Todas las consultas son del **2026-09-24**, salvo donde se indica lo contrario.

Plataforma y herramientas de prueba

1. API de releases de GitHub (JSON crudo vía herramienta de fetch): `microsoft/playwright` (latest),
   `cypress-io/cypress` (latest), `vitest-dev/vitest` (últimas 5), `pytest-dev/pytest` (latest),
   `dequelabs/axe-core` (latest), `junit-team/junit-framework` (latest), `jestjs/jest` (latest),
   `SeleniumHQ/selenium` (latest).
2. W3C WebDriver BiDi — commits del Editor's Draft, API de GitHub:
   `api.github.com/repos/w3c/webdriver-bidi/commits` con
   `since=2026-09-21T00:00:00Z&until=2026-09-24T23:59:59Z` (leída; 2 commits, sin solape con la ventana
   del informe anterior — ver H2).
3. Node.js — Avisos de vulnerabilidad: https://nodejs.org/en/blog/vulnerability (leída).
4. CSA Singapur — Índice de advisories: https://www.csa.gov.sg/alerts-and-advisories/advisories/
   (leído).
5. W3C — WCAG 2.2 Errata: https://www.w3.org/WAI/WCAG22/errata/ (leída, sólo el sello y la lista de
   entradas; no se repitió la lectura del texto vivo de §5.2.3 ni de `#dfn-process` — sin novedad
   esperable en tres días, ver pregunta abierta 7).
6. Google — Privacy Sandbox feature status: https://privacysandbox.google.com/overview/status (leída).
7. OWASP Top 10 2025: https://owasp.org/Top10/2025/0x00_2025-Introduction/ → redirect 308 a
   `https://top10.owasp.org/2025/0x00_2025-Introduction` (leída).
8. GitHub Changelog — índice: https://github.blog/changelog/ (leído, rango 2026-09-21 a 2026-09-24) y el
   detalle de una entrada:
   `github.blog/changelog/2026-09-23-node-20-is-no-longer-available-in-github-actions/` (leída).

Comandos y lecturas locales: `git status --porcelain` (dos veces, antes y después de `make
agent-evaluate`). `git log -n 5 --format='%cs %H %s' -- agents/roles/system/qa-engineer/SKILL.md` → sin
cambios desde el 2026-09-02, commit `3b1a4352` (mismo que registró el informe anterior). `git log -n 15
--format='%cs %H %s' -- agents/roles/system/qa-engineer/` → historial de commits del cargo, entre ellos
`97d93534` (H8). `git show 97d93534 -- agents/roles/system/qa-engineer/learning/AUTOMATION.md` → diff
completo citado en H8. Lectura de `docs/issues/182-la-investigacion-semanal-no-adelanta-nada-que-la-mensual-no-traiga.md`
(frontmatter `estado: resuelto`, `resuelto-en: 0.98.0`). Lectura de `package.json` → `"version": "0.99.0"`.
Lectura de `.github/workflows/agent-learning.yml`, bloque `on:` → cron mensual `17 13 24 * *`, sin cron
semanal. `grep -rn "node-version|actions/setup-node" .github/workflows/` → los siete workflows usan Node
'24' o `matrix.node: ["24", current]`, ninguno Node 20 (H7). `grep -rn "codeql" .github/workflows/ -i` →
sin resultados. `make agent-evaluate AGENT=qa-engineer` (`node engine/cli/ops.js evaluate qa-engineer`) →
salida completa citada en H9.

Consultas que no se repitieron esta corrida y por qué (R20): ISO/IEC 40500:2025, ISO/IEC 25059, EN 301
549 V4.1.1, syllabus de ISTQB CTFL/CT-AI, OWASP Web Security Testing Guide, texto vivo de WCAG 2.2 §5.2.3
y `#dfn-process`, documentación dedicada de `cypress tap`, tipo del `NPM_TOKEN` de respaldo —ninguna tiene
señal de cambio esperable en una ventana de tres días, y las dos últimas no son comprobables desde este
entorno (preguntas abiertas 1, 2, 3, 5, 7, 10).

## Hallazgos

### H1 — Cero releases nuevas en las nueve piezas de referencia en esta ventana de tres días

Confianza: **alta** (campos escalares de la API de releases de cada repositorio oficial — registro
**verificado**).

| Herramienta | Última versión estable | Fecha | Cambio desde 2026-09-21 |
|---|---|---|---|
| Cypress | 16.1.0 | 2026-09-15 | sin cambios |
| Vitest | 5.0.1 | 2026-09-15 | sin cambios |
| Jest | 30.5.2 | 2026-09-18 | sin cambios |
| Selenium | 4.49.0 | 2026-09-09 | sin cambios |
| Playwright | 1.63.0 | 2026-09-04 | sin cambios |
| pytest | 9.1.1 | 2026-06-19 | sin cambios |
| JUnit | r6.1.3 | 2026-08-07 | sin cambios |
| axe-core | 4.13.0 | 2026-08-05 | sin cambios |
| Node.js | sin release de seguridad posterior al 2026-07-29 | 2026-07-29 | sin cambios (ver H3) |

Los mismos nueve valores que registró el informe del 2026-09-21. Ninguna de las tres herramientas que
habían tenido release esa semana (Cypress, Vitest, Jest) publicó una nueva en estos tres días.

### H2 — WebDriver BiDi: dos commits nuevos, sin el artefacto de solape de ventanas que afectó a informes anteriores

Aplica a: W3C WebDriver BiDi, repositorio `w3c/webdriver-bidi`. Confianza: **alta** para el conteo
(**verificado**, API de GitHub); **documentado** para el contenido de los mensajes de commit, no leídos
en diff.

La consulta con `since=2026-09-21T00:00:00Z&until=2026-09-24T23:59:59Z` devolvió dos commits: «Pass a new
`hasPlannedNavigation` flag to the `browsingContext.contextCreated` event» (issue #1151,
2026-09-24T14:48:43Z) y «Update Web Bluetooth link to its WHATWG URL» (2026-09-23T05:57:18Z, cambio de
documentación, no de comportamiento). A diferencia de los dos informes anteriores, esta ventana empieza
exactamente donde terminó la anterior (`since=2026-09-21T00:00:00Z` contra el `until=2026-09-21T00:00:00Z`
del informe pasado), así que no hay commit duplicado que descontar. Sigue sin cambiar la conclusión ya
sostenida: la cadencia del Editor's Draft es irregular en el agregado.

### H3 — Sin aviso de seguridad nuevo de Node.js ni advisory nuevo de CSA Singapur

Confianza: **alta** (**verificado**) para los dos. Node.js sigue listando el 2026-07-29 como el aviso más
reciente; CSA Singapur sigue listando AD-2026-011 (26 de agosto de 2026) como el más reciente, sin entrada
posterior. Sin novedad esta ventana en ninguno de los dos.

### H4 — WCAG 2.2 Errata: mismo sello, sin entradas nuevas

Confianza: **alta** (**verificado**) para el sello y la ausencia de entradas nuevas. El sello de la
página de erratas sigue en **«$Date: 2026/09/03 15:18:01 $»**, sin cambio respecto de los cuatro informes
anteriores, y la entrada más reciente sigue siendo la del 2026-08-17. No se repitió la lectura del texto
vivo de §5.2.3 ni de `#dfn-process` esta corrida (R20): la pregunta abierta 7 sobre la fidelidad byte a
byte de esas citas sigue exactamente donde la dejó el informe del 2026-09-21, sin dato nuevo.

### H5 — Privacy Sandbox: mismo sello, ahora 41 días después

Confianza: **alta** (**verificado**). La página de estado de Privacy Sandbox muestra el mismo sello
**«Last Updated: August 14, 2026»** que en los seis informes anteriores, con Topics API y Attribution
Reporting API todavía en «Deprecate and remove», sin dato de porcentaje de despliegue. Sin fuente nueva
para la pregunta abierta 4.

### H6 — OWASP Top 10 2025: mismo redirect, sin dato nuevo sobre el estado de edición

Confianza: **alta** (**verificado**). `https://owasp.org/Top10/2025/0x00_2025-Introduction/` sigue
redirigiendo (308) a `https://top10.owasp.org/2025/0x00_2025-Introduction`, sin cambio respecto del
informe del 2026-09-21. La pregunta de fondo (edición final o borrador) sigue sin dato nuevo; ver pregunta
abierta 9.

### H7 — GitHub Changelog: Node 20 se retira de los runners de GitHub Actions; sin impacto en este repositorio

Confianza: **alta** para la cita (**documentado**: leída directo de la página del changelog); **alta**
para la verificación local de que no aplica a este repositorio (**verificado**).

Cita literal: *"Node 20 is no longer available on GitHub Actions runners. Runners now use Node 24 for
JavaScript actions."* (2026-09-23). El aviso recomienda a quien mantiene una action actualizar
`runs.using` a `node24`, y a quien consume actions, actualizar a versiones que ya lo soporten. Comprobado
contra este repositorio: los siete workflows de `.github/workflows/` fijan `node-version: '24'` (o, en
`ci.yml`, la matriz `["24", current]`) en cada uso de `actions/setup-node` — ninguno pide Node 20. Ningún
workflow de este repositorio usa CodeQL (`grep -i codeql` sin resultados), así que la otra entrada del
índice de esa semana («Deprecation notice: All-platform CodeQL bundle») tampoco alcanza a este
repositorio. Lo que este hallazgo no comprueba es el `runs.using` interno de cada action de terceros que
los workflows referencian por SHA — eso excede lo que una invocación inocua puede establecer y no se
afirma.

No es un hallazgo de la profesión de QA — es contexto operativo de CI, igual que H9 del informe del
2026-09-21 — y se registra acá porque toca directamente los workflows de este mismo repositorio.

### H8 — La cadencia de investigación pasó de semanal a mensual: esta corrida es la primera bajo el cron nuevo

Confianza: **alta** (**verificado**, todo local). El commit `97d93534` («docs(agents): schedule each
agent's research monthly», 2026-09-22) cambió `agents/roles/system/qa-engineer/learning/AUTOMATION.md` de
«Automatización semanal del aprendizaje» / «Programar semanalmente en cada instalación» a
«Automatización mensual del aprendizaje» / «Programar mensualmente en cada instalación» — el mismo
cambio se aplicó en los 54 archivos que el commit toca, entre ellos `AGENTS.md` y las 53 `AUTOMATION.md`
del catálogo. Es la aplicación de `docs/issues/182` («La investigación semanal no adelanta nada que la
mensual no traiga…», frontmatter `estado: resuelto`, `resuelto-en: 0.98.0`), y este repositorio corre
`0.99.0` (`package.json`), una versión posterior. El cron de `.github/workflows/agent-learning.yml`
confirma el cambio en el mecanismo que dispara la corrida: `17 13 24 * *` (mensual, el día 24 de cada
mes) y `17 13 23 3,6,9,12 *` (trimestral), sin ningún cron semanal — el caso 182 documenta en su cierre
que el cron del lunes se quitó junto con la opción manual `semanal` del `workflow_dispatch`. Hoy es
2026-09-24: esta corrida coincide con la fecha del cron mensual.

El propio caso 182 deja a propósito sin tocar la redacción «Guardar informes semanales» de los 53
`SKILL.md` del catálogo —incluido el de este cargo—, para no marcar sus contratos como cambiados; ese
comentario ya lo explica el commit y no requiere ninguna nota nueva de este cargo.

### H9 — Sin propuesta ni resultado de evaluación nuevos desde el informe anterior; `agent-evaluate` reporta ahora tres informes sin consolidar

Confianza: **alta** (**verificado**, ambos leídos directo). Salida completa de `node engine/cli/ops.js
evaluate qa-engineer`:

```
⚠ 3 informe(s) sin consolidar (2026-09-14.md, 2026-09-21.md, 2026-09-24.md): entran en la próxima propuesta
⚠ el contrato cambió el 2026-09-02 y el veredicto más viejo es del 2026-09-01: mide una versión anterior
✓ qa-engineer: 13 caso(s) — 13/13 pasan (2026-09-01…2026-09-16), 7 propuesta(s), controles estructurales válidos
```

Idéntica al resultado agregado del informe del 2026-09-21 (13/13, mismo rango de veredictos
2026-09-01…2026-09-16, mismas dos advertencias estructurales), salvo que la lista de informes sin
consolidar creció de dos a tres al sumarse este mismo informe. `git log` sobre
`evaluations/results/` y `learning/proposals/` no muestra ningún archivo nuevo desde el
2026-09-16 y el 2026-09-14 respectivamente: nadie corrió una evaluación nueva ni escribió una propuesta
en estos tres días. Los dos huecos de contrato que el informe del 2026-09-21 encontró en «Para el
contrato» de `evaluations/results/2026-09-16.md` (H11 de ese informe) siguen exactamente como quedaron:
candidatos sin decidir, a la espera del ensamblaje mensual del 2026-10-01, que es la primera vez que ese
mecanismo corre bajo la cadencia nueva.

## Evidencia

Todo lo afirmado arriba se verificó el **2026-09-24** contra la fuente enumerada en «Fuentes
consultadas», salvo donde el propio hallazgo marca otro registro.

- **H1** — Cypress: `tag_name: "v16.1.0"`, `published_at: "2026-09-15T21:37:56Z"`. Vitest: `tag_name:
  "v5.0.1"`, `published_at: "2026-09-15T08:43:24Z"`, `prerelease: false`. Jest: `tag_name: "v30.5.2"`,
  `published_at: "2026-09-18T13:57:03Z"`. Selenium: `selenium-4.49.0`, `2026-09-09T15:35:47Z`.
  Playwright: `v1.63.0`, `2026-09-04T22:40:31Z`. pytest: `9.1.1`, `2026-06-19T10:58:55Z`. JUnit:
  `r6.1.3`, `2026-08-07T13:11:01Z`. axe-core: `v4.13.0`, `2026-08-05T16:52:19Z`. Los mismos ocho valores
  que registró el informe del 2026-09-21.
- **H2** — `api.github.com/repos/w3c/webdriver-bidi/commits?since=2026-09-21T00:00:00Z&until=2026-09-24T23:59:59Z`
  → 2 commits (SHA `318aec70`, `0bdadfff`), mensajes citados en el hallazgo.
- **H3** — `nodejs.org/en/blog/vulnerability`: entrada más reciente «Wednesday, July 29, 2026 Security
  Releases», sin entrada posterior. Índice de CSA: entrada más reciente AD-2026-011 (26 de agosto de
  2026), sin entrada posterior.
- **H4** — `w3.org/WAI/WCAG22/errata/`: sello `$Date: 2026/09/03 15:18:01 $`, sin entradas posteriores al
  2026-08-17.
- **H5** — Privacy Sandbox: «Last Updated: August 14, 2026», sin cambio respecto de los seis informes
  anteriores.
- **H6** — Redirect reportado por la herramienta de fetch: `https://owasp.org/Top10/2025/0x00_2025-Introduction`
  → 308 → `https://top10.owasp.org/2025/0x00_2025-Introduction`, mismo resultado que el informe anterior.
- **H7** — Cita literal transcrita en el hallazgo, de
  `github.blog/changelog/2026-09-23-node-20-is-no-longer-available-in-github-actions/`. Verificación
  local: `grep -rn "node-version" .github/workflows/` → los siete workflows fijan `'24'` o
  `matrix.node: ["24", current]`; `grep -rni "codeql" .github/workflows/` → sin resultados.
- **H8** — `git show 97d93534 -- agents/roles/system/qa-engineer/learning/AUTOMATION.md`: diff de
  «Automatización semanal…» / «Programar semanalmente…» a «Automatización mensual…» / «Programar
  mensualmente…», transcrito en el hallazgo. `docs/issues/182-…md`, frontmatter `estado: resuelto`,
  `resuelto-en: 0.98.0`. `package.json` → `"version": "0.99.0"`. `.github/workflows/agent-learning.yml`,
  bloque `on.schedule`: `17 13 24 * *` (mensual), `17 13 23 3,6,9,12 *` (trimestral), `17 7 1 * *`
  (ensamblaje), sin cron semanal.
- **H9** — Salida completa de `node engine/cli/ops.js evaluate qa-engineer` transcrita en el hallazgo;
  ausencia de archivos nuevos en `evaluations/results/` y `learning/proposals/` desde el 2026-09-16 y el
  2026-09-14 respectivamente, comprobada con `git log` sobre ambos directorios.

Limitaciones de esta corrida:

- El entorno declarado (Node v24.21.0, npm 11.19.0) es un dato del encargo de esta corrida, sin
  re-chequeo independiente por instrucción explícita — a diferencia de las corridas anteriores, que sí
  corrieron `node -v`.
- No se repitió la lectura del texto vivo de WCAG 2.2 §5.2.3 ni de `#dfn-process` (pregunta abierta 7):
  sin cambio de sello en la errata, no había señal de que fuera a haber novedad en tres días.
- No se comprobó el `runs.using` interno de las actions de terceros que los workflows de este
  repositorio referencian por SHA frente al retiro de Node 20 (H7): excede lo que una invocación inocua
  puede establecer.
- Si el `NPM_TOKEN` de respaldo de este repositorio es del tipo bypass-2FA que npm apunta a dejar de
  aceptar en enero de 2027 sigue sin ser algo que este cargo pueda comprobar desde este entorno.

## Posibles prácticas obsoletas

1. **Configurar `deps.optimizer.web` en Vitest.** Sigue deprecada desde 5.0.1 (informe 2026-09-21, H3);
   sin cambio esta ventana.
2. Ningún ítem nuevo esta corrida. Las de los informes anteriores (Selenium Grid, `cache-mode` de GitHub
   Actions) siguen valiendo sin cambios; ver el informe del 2026-09-14 para el detalle.

## Recomendación

Para la propuesta mensual de `learning/proposals/`, sujeta a aprobación humana.

**1. Ningún hallazgo de esta corrida pide tocar `sources.yaml`, `SKILL.md`, `references/` ni un caso.**
Es la razón del `propone: no` de este informe: la ventana de tres días no trajo release, deprecación,
advisory ni cambio normativo que el contrato de QA tenga que absorber. Los dos candidatos concretos para
`evaluations/expected-behaviors.yaml` siguen siendo los que nombró H11 del informe del 2026-09-21 — no se
repiten acá; ese informe ya trae el enunciado propuesto por el propio juez de la evaluación.

**2. El ensamblaje del 2026-10-01 es el primero que corre bajo la cadencia mensual y va a consolidar tres
informes de una vez** (2026-09-14, 2026-09-21, 2026-09-24 — H9), no uno. Vale la pena que quien revise esa
propuesta sepa que el volumen es mayor por diseño (caso 182), no porque algo se haya acumulado sin
atenderse.

**3. H7 (retiro de Node 20 en GitHub Actions) es contexto operativo de CI, no del contrato de la
profesión, igual que H9 del informe del 2026-09-21 con las protecciones de `pull_request_target`.** No
requiere acción de este repositorio, que ya corre Node 24 en los siete workflows.

**4. H8 no requiere ninguna nota operativa nueva de este cargo:** el cambio de cadencia ya está resuelto
y documentado como caso 182 del propio toolkit, con su commit, su cron y su versión de resolución.

## Preguntas abiertas

1. **¿Qué edición de WCAG 2.2 quedó incorporada en ISO/IEC 40500:2025?** Sigue abierta; no rechequeada
   esta corrida (informes 2026-08-27, 2026-08-22).
2. **¿En qué etapa formal está la revisión de ISO/IEC 25059?** Sigue abierta, sin fuente primaria
   accesible; no rechequeada esta corrida (informe 2026-08-22, H2).
3. **¿Cuándo se cita EN 301 549 V4.1.1 en el DOUE, si es que se publica?** Sigue abierta desde hace
   varios informes (2026-08-22, H8); sin señal de cambio.
4. **¿La remoción de Topics API y Attribution Reporting API en Chrome alcanzó al 100% de usuarios
   estables?** Sigue abierta (informe 2026-08-27, H1); el sello de la fuente pública consultada sigue sin
   cambio 41 días después (H5).
5. **¿Hay documentación de `cypress tap` más allá de la guía pública consultada?** Sigue abierta (informe
   2026-08-27, H4); sin release nueva de Cypress esta ventana (H1), así que sin dato nuevo.
6. **¿Quién exporta o archiva, antes del 2026-10-01, el historial de Actions que sirve de evidencia de
   pipeline?** (informe 2026-08-29, H5). Sigue siendo pregunta para DevOps/SRE por repositorio; el plazo
   nombrado vence dentro de una semana de esta corrida.
7. **¿Qué cambió exactamente el texto vivo de WCAG 2.2 §5.2.3 con la errata del 2026-08-17 —el ejemplo de
   la definición de «process»?** (informe 2026-09-07, H8; tercer intento sin confirmar en el informe del
   2026-09-21, H7). No rechequeada esta corrida (R20): sin cambio de sello en la errata, sin señal de que
   una cuarta lectura fuera a variar.
8. **¿Es «documentado» o «verificado» razonable como registro por defecto para los resúmenes de prosa que
   devuelve la herramienta de fetch?** (informe 2026-09-07). Sin dato nuevo esta corrida — todas las
   lecturas de prosa de esta ventana (H7) se registraron como documentado, sin intento de verificación
   byte a byte. Sigue siendo decisión de quien opere el ciclo de aprendizaje si conviene fijarlo como
   práctica.
9. **¿Cuál es el estado real de edición del OWASP Top 10 2025 —final o borrador?** Sigue sin establecerse
   (H6); sin dato nuevo esta corrida.
10. **¿El `NPM_TOKEN` de respaldo que `AGENTS.md` describe para `release.yml` es del tipo bypass-2FA que
    npm apunta a dejar de aceptar en enero de 2027?** (informe 2026-09-21, H9). No comprobable desde este
    entorno: depende de la configuración de la cuenta de npm en el registro, fuera del alcance de R12
    para este cargo.
11. **¿Cómo se comporta en la práctica el primer ensamblaje mensual del 2026-10-01, que consolida tres
    informes en vez de uno?** Nueva esta corrida (H8, H9). No es una pregunta sobre la profesión de QA
    sino sobre el propio ciclo de aprendizaje del cargo; la respuesta la va a dar esa corrida, no ésta.
12. **Contexto de empresa**: este informe cubre la profesión y la infraestructura de evaluación de este
    repositorio, no una instalación. No evalúa `organization/roles/qa-engineer.md` ni el contexto de
    pruebas de ningún proyecto concreto.
