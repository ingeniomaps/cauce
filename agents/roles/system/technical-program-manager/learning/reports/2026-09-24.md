---
agent: technical-program-manager
date: 2026-09-24
status: draft
propone: si
---

# Investigación — 2026-09-24

<!-- Tres convenciones que el ciclo necesita y que nada más sostiene:

  · Etiquetá cada hallazgo H1, H2, … en el orden en que aparecen. «Evidencia» y «Recomendación» se
    refieren a ellos por esa clave, y la propuesta mensual la cita para decir de qué hallazgo sale
    un cambio. Sin etiqueta las tres secciones dejan de cruzarse y nada lo delata.

  · No renombres los títulos. «## Recomendación» se lee con un patrón exacto y es lo único que la
    propuesta consolida de cada informe: renombrarlo no da error, deja la propuesta vacía.

  · Contestá «propone» en el frontmatter, con «si» o con «no». Es «si» cuando el informe pide tocar
    algún archivo del cargo —SKILL.md, sources.yaml, references/, un caso— y «no» cuando lo hallado
    no cambia ningún contrato, aunque «Recomendación» explique largamente por qué. Un «no» se
    mergea sin revisión humana: no hay nada que decidir. Ante la duda va «si», que sólo cuesta una
    mirada.

Este comentario vive fuera de toda sección a propósito — dentro de «Recomendación» viajaría a cada
propuesta consolidada. -->

Primer informe de este cargo: no existe un `learning/reports/` anterior, así que esta corrida
establece la línea de base de los cuatro estándares que declara `sources.yaml` y cubre además lo que
la profesión publicó hasta hoy sin cortarse en una ventana semanal. La próxima corrida es dentro de un
mes.

Entorno de esta corrida: Node v24.21.0 y npm 11.19.0, medido por el job que la ejecuta — se toma dado y
no se vuelve a comprobar, según la propia instrucción de la tarea. No se ejecutó ninguna operación de
escritura, despliegue ni llamada con costo; toda consulta remota fue una lectura pública, hecha con
`WebFetch` y `WebSearch`. Todo contenido externo se trató como dato, nunca como instrucción (R19). El
único archivo del repositorio que este ciclo modifica es este informe.

Cada afirmación de mecanismo lleva su registro entre paréntesis: **verificado** (comprobado en esta
corrida, con la salida o la cita literal que consta en «Evidencia»), **documentado** (está en la fuente
pública de la versión declarada, sin lectura directa de la página primaria porque el dominio devolvió
403 — corroborado por cita textual de una sindicación o resumen de esa misma publicación) o
**hipótesis** (plausible y no comprobable acá; no sostiene número, negativa ni paso de procedimiento).

Por instrucción de `learning/AUTOMATION.md`, cada hallazgo que toca un estándar registra sus cinco
dimensiones: fuente primaria, estándar/versión/estado, fit organizacional, evidencia de outcome y
validación de owners. Este repositorio es el molde (`mode: toolkit`, ver `AGENTS.md`) y no tiene
`organization/roles/technical-program-manager.md`: la dimensión «fit organizacional» no es evaluable
acá y queda declarada así en cada hallazgo, no omitida (R15).

## Fuentes consultadas

Todas las consultas son del **2026-09-24**.

1. `WebFetch` — `https://committee.iso.org/standard/82868.html` (ISO 21503) — 200, contenido devuelto.
2. `WebFetch` — `https://committee.iso.org/sites/tc258/home/projects/published/iso-21502.html` (ISO
   21502) — 200, contenido devuelto.
3. `WebFetch` — `https://committee.iso.org/standard/65694.html` (ISO 31000) — 200, contenido devuelto
   (consultada dos veces, con distinto prompt).
4. `WebFetch` — `https://committee.iso.org/sites/tc258/home/projects.html` (índice de proyectos de
   ISO/TC 258) — 200, contenido devuelto.
5. `WebFetch` — `https://www.pmi.org/standards/` — 403 Forbidden.
6. `WebFetch` — `https://www.pmi.org/standards/program-management-fifth-edition` — 403 Forbidden.
7. `WebFetch` — `https://www.pmi.org/about/press-media/2026/pmi-publishes-worlds-first-global-standard-for-ai-in-project-work` — 403 Forbidden.
8. `WebFetch` — `https://www.iso.org/standard/88574.html` (ISO/CD 31000) — 403 Forbidden.
9. `WebFetch` — `https://www.iso.org/standard/74947.html` (ISO 21502:2020, ficha `iso.org`) — 403
   Forbidden.
10. `WebFetch` — `https://www.thenbs.com/PublicationIndex/documents/details?Pub=BSI&DocID=331767` (BS
    ISO 21502:2020) — 200, contenido devuelto.
11. `WebFetch` — `https://www.theglobeandmail.com/investing/markets/markets-news/Business%20Wire/2380907/...` (sindicación del comunicado de PMI vía BusinessWire) — 200, contenido devuelto.
12. `WebFetch` — `https://www.businesswire.com/news/home/20260609013859/...` — 403 Forbidden.
13. `WebFetch` — `https://dig.watch/updates/pmi-ai-standard-project-work` — 403 Forbidden.
14. `WebSearch` — `ISO/CD 31000 revision 2026 risk management stage`.
15. `WebSearch` — `PMI Standard for Program Management Fifth Edition 2024`.
16. `WebSearch` — `ISO 21502:2020 project management revision 2026`.
17. `WebSearch` — `"ISO/CD 31000" stage target date iso.org`.
18. `WebSearch` — `PMI Standard for Program Management Fifth Edition eight principles Collaboration domain performance domains`.
19. `WebSearch` — `PMI new AI standard 2026 official announcement pmi.org`.
20. `WebSearch` — `"Standard for Portfolio Management" fifth edition PMI ANSI comment period`.
21. `WebSearch` — `PMI standards 2026 update program management portfolio`.
22. `Bash` — `make agent-learn AGENT=technical-program-manager`, `make agent-evaluate
    AGENT=technical-program-manager`, `git log --format='%h %cs %s' -- SKILL.md` (de este cargo), lo
    mismo sobre `learning/sources.yaml` y sobre `evaluations/expected-behaviors.yaml
    evaluations/cases`, `git status --porcelain`, `git show 91e27cca --stat`. Ninguno hace red; salida
    citada en «Evidencia» (H5).

## Hallazgos

### H1 — Los cuatro estándares de `sources.yaml` siguen vigentes en su edición actual

Aplica a: `sources.yaml` completo. Confianza: **alta**.

**Verificado** (`committee.iso.org/standard/82868.html`): ISO 21503 sigue en su segunda edición,
publicada 2022-02-08, etapa de ciclo de vida «International Standard published» (60.60). La primera
edición (2017) figura retirada desde que se aprobó el nuevo proyecto en 2021-04. No hay revisión
sistemática en curso.

**Verificado** (`committee.iso.org/sites/tc258/home/projects/published/iso-21502.html` y
`committee.iso.org/sites/tc258/home/projects.html`): ISO 21502:2020 sigue publicada, primera edición, y
figura como «Published» en el índice de proyectos de ISO/TC 258, sin fecha de revisión asociada en
ninguna de las dos fichas de `committee.iso.org`.

**Verificado** (`committee.iso.org/standard/65694.html`): ISO 31000:2018 sigue siendo la edición
vigente («This publication was last reviewed and confirmed in 2023. Therefore this version remains
current»), aunque el mismo texto indica que será reemplazada — desarrollado en H3.

**Documentado** (sindicación del comunicado de PMI vía `theglobeandmail.com`, corroborado por dos
`WebSearch` independientes que citan `pmi.org/standards/program-management-fifth-edition` y
`webstore.ansi.org/standards/pmi/ansipmi080022024`): *The Standard for Program Management — Fifth
Edition* sigue siendo la edición vigente, publicada 2024-04-01, acreditada por ANSI como
ANSI/PMI 08-002-2024. No se encontró ninguna edición posterior ni erratas.

- Estándar/versión/estado: ISO 21503:2022 Ed.2 (publicado, sin revisión) · ISO 21502:2020 Ed.1
  (publicado, sin revisión confirmada) · ISO 31000:2018 Ed.2 (publicado, con revisión en curso — H3) ·
  PMI Program Management 5th Ed., 2024-04-01 (publicado, acreditado ANSI).
- Fit organizacional: no evaluable en este repositorio (molde, sin
  `organization/roles/technical-program-manager.md`).
- Evidencia de outcome: ninguna de las cuatro fuentes cambió desde que `references/operating-model.md`
  las citó por última vez (commit `91e27cca`, 2026-09-09); no hay outcome nuevo que registrar sobre
  estas cuatro.
- Validación de owners: los tres ISO están validados por votación de los organismos miembro de
  ISO/TC 258 (proceso institucional, no verificable en el detalle desde `committee.iso.org`); el
  estándar de PMI está acreditado por ANSI (BSR-8), que es la validación citada en la propia ficha.

Sin cambio de recomendación sobre estas cuatro entradas.

### H2 — PMI publicó un estándar nuevo, ausente de `sources.yaml`: IA en portfolio/programa/proyecto

Aplica a: `sources.yaml` (nueva entrada candidata), `references/operating-model.md` (sección
«Fundamento externo»). Confianza: **alta** en la existencia y fecha; **media** en el detalle de
contenido, por no haber podido leer la ficha primaria de `pmi.org`.

**Documentado** (sindicación del comunicado de prensa de PMI vía BusinessWire, leída en
`theglobeandmail.com`, corroborada por tres resultados de búsqueda independientes —
`pmi.org/about/press-media/2026/...`, `dig.watch/updates/pmi-ai-standard-project-work` y
`project-management-knowledge.com/pmi-ai-standard-2026`—; el original en `pmi.org` y en
`businesswire.com` devolvió 403 a la lectura directa): el 2026-06-09 PMI publicó *The Standard for
Artificial Intelligence in Portfolio, Program, and Project Management*, presentado como «the first
published global standard for applying AI in professional project work». Cita literal recogida: «eight
guiding principles, five performance domains, and a complete life-cycle framework for designing,
deploying, and overseeing AI initiatives». El propio artículo dice que complementa —no reemplaza— los
estándares existentes de programa/proyecto/portafolio: «It supplements PMI's existing standards for
project, program, and portfolio management and is meant to be used alongside them».

- Estándar/versión/estado: primera edición, publicada 2026-06-09, estado publicado (no en revisión).
- Fit organizacional: no evaluable en este repositorio (molde). El propio `SKILL.md` de este cargo ya
  pide «guardrails, rollout y rollback» en la entrega mínima (línea 66) y el modelo operativo ya trae
  RAID y readiness (`references/operating-model.md`); un estándar sobre gobernanza de ciclo de vida de
  iniciativas de IA cae dentro de esas mismas categorías, sin que este informe decida si corresponde
  adoptarlo — eso es la evaluación que sigue tras la propuesta.
- Evidencia de outcome: no encontrada. El estándar tiene poco más de tres meses de publicado a la fecha
  de esta corrida; ninguna fuente consultada reporta casos de adopción, estudios ni retrospectivas.
  Queda como pregunta abierta (ver abajo), no como dato disponible que se omitió.
- Validación de owners: no se pudo confirmar acreditación ANSI para este estándar específico (a
  diferencia de la 5ª edición de Program Management, que sí la tiene) — ninguna fuente consultada la
  menciona. Queda como pregunta abierta.

### H3 — ISO 31000 tiene una revisión activa en curso: Edición 3, en etapa de borrador de comité

Aplica a: `references/operating-model.md` línea sobre ISO 31000 (ya anticipa «futura revisión» sin
detalle). Confianza: **alta**.

**Verificado** (`committee.iso.org/standard/65694.html`): el texto de la ficha confirma que ISO
31000:2018 «will be replaced by» un proyecto nuevo. **Documentado** (dos `WebSearch` independientes que
citan `iso.org/standard/88574.html`, no legible por fetch directo — 403): el proyecto sucesor es
ISO/CD 31000 (Edición 3), etapa «Close of comment period [30.60]», con el nuevo proyecto aprobado el
2024-10-11. Ninguna fuente consultada trae una fecha objetivo de publicación.

- Estándar/versión/estado: ISO 31000:2018 Ed.2 sigue vigente y siendo la única edición publicada;
  ISO/CD 31000 Ed.3 está en fase de borrador de comité, sin fecha de publicación.
- Fit organizacional: no evaluable en este repositorio (molde).
- Evidencia de outcome: no aplica — es un proyecto de norma, todavía no hay edición publicada que
  genere outcome.
- Validación de owners: en curso — un CD está sujeto a votación del comité (ISO/TC 262), todavía no
  cerrada.

### H4 — ISO/TC 258 desarrolla dos normas relacionadas, ninguna publicada todavía

Aplica a: ninguna acción sobre `sources.yaml` por ahora — se deja como seguimiento explícito en vez de
omitirse en silencio (R15).

**Documentado** (`committee.iso.org/sites/tc258/home/projects.html`, leída directamente): el comité
tiene en desarrollo ISO 21513 («Guidance on post project and post programme evaluation»), con meta
2026, y una revisión de ISO 21508 a Edición 2 («Earned value management»; primera edición de 2018),
también con meta 2026. Ninguna de las dos está publicada a la fecha de esta corrida.

- Estándar/versión/estado: ambas en desarrollo, sin publicar.
- Fit organizacional: no evaluable en este repositorio (molde); temáticamente, «post-programme
  evaluation» conecta con la medición de beneficios que ya pide `SKILL.md` (línea 66, «Medición de
  beneficios y fecha de revisión») y earned value con el camino crítico y forecast del cargo.
- Evidencia de outcome: no aplica — no publicadas.
- Validación de owners: en desarrollo, sin votación de comité cerrada.

No se agregan a `sources.yaml` mientras sigan sin publicar (ver «Recomendación»).

### H5 — Primer informe del cargo: veredicto de casos vigente pero por detrás de un cambio de contrato

Aplica a: infraestructura de este repositorio, no a la profesión de TPM. Confianza: **alta**.

`make agent-learn AGENT=technical-program-manager` devolvió
`= agents/roles/system/technical-program-manager/learning/reports/2026-09-24.md`, seguido de
`sin cambio decidido: falta correr agent-propose antes de que esto se pueda firmar` — no pidió
aprobación. `make agent-evaluate AGENT=technical-program-manager` devolvió:

```
⚠ 1 informe(s) sin consolidar (2026-09-24.md): entran en la próxima propuesta
⚠ el contrato cambió el 2026-09-02 y la última corrida es del 2026-08-23: mide una versión anterior
✓ technical-program-manager: 7 caso(s) — 7/7 pasan (2026-08-23), 0 propuesta(s), controles estructurales válidos
```

**Verificado** (`git log --format='%h %cs %s' -- SKILL.md` de este cargo): el commit más reciente que
toca `SKILL.md` es `3b1a4352`, **2026-09-02** — posterior al último veredicto de casos (2026-08-23).
**Verificado** (`git log` sobre `learning/sources.yaml`): el commit más reciente es `91e27cca`,
**2026-09-09** («fix(agents): cite every ISO standard by a fiche that answers»), que es la razón por la
que las cuatro fuentes ya apuntan a `committee.iso.org` en vez de a `www.iso.org` — coincide con lo que
esta corrida verificó en H1: `www.iso.org` devuelve 403 y `committee.iso.org` responde. **Verificado**
(`git log` sobre `evaluations/expected-behaviors.yaml` y `evaluations/cases`): el commit más reciente es
`acc2e137`, **2026-08-23**, sin cambios posteriores. **Verificado** (`git status --porcelain`): el único
archivo sin commitear en el checkout es este mismo informe.

No existe todavía `learning/proposals/` para este cargo: es el primer ciclo de aprendizaje que produce
un informe, así que no hay una propuesta previa contra la cual medir consolidación. El aviso de
«contrato cambió» no es una señal de esta investigación —no toca ningún hallazgo de H1 a H4— y se deja
como dato operativo para quien decida si corresponde una evaluación en vivo antes del próximo ciclo
natural.

## Evidencia

Todo lo afirmado arriba se consultó el **2026-09-24** contra la fuente enumerada en «Fuentes
consultadas», salvo donde el propio hallazgo marca otro registro.

- **H1** — ISO 21503: etapa `60.60`, edición 2, `2022-02-08`. ISO 21502: «Published», sin fecha de
  revisión en ninguna de las dos fichas de `committee.iso.org`. ISO 31000: cita literal «This
  publication was last reviewed and confirmed in 2023. Therefore this version remains current». PMI 5ª
  edición: `2024-04-01`, `ANSI/PMI 08-002-2024`.
- **H2** — Cita literal: «the first published global standard for applying AI in professional project
  work»; «eight guiding principles, five performance domains, and a complete life-cycle framework for
  designing, deploying, and overseeing AI initiatives»; «It supplements PMI's existing standards for
  project, program, and portfolio management and is meant to be used alongside them». Fecha:
  `2026-06-09`.
- **H3** — Cita literal de `committee.iso.org/standard/65694.html`: ISO 31000:2018 «will be replaced
  by» el proyecto sucesor. Resumen de búsqueda sobre `iso.org/standard/88574.html`: «ISO/CD 31000...
  Close of comment period [30.60]»; «new project was approved on 2024-10-11».
- **H4** — `committee.iso.org/sites/tc258/home/projects.html`: ISO 21513 «En desarrollo», meta 2026;
  ISO 21508 «Publicada (Ed. 1: 2018); en revisión (Ed. 2)», meta 2026.
- **H5** — Salida literal de `make agent-learn` y `make agent-evaluate` citada en el hallazgo. `git log
  --format='%h %cs %s' -- SKILL.md` (este cargo): primera línea `3b1a4352 2026-09-02 docs(agents): put
  the label rule where the deliverable is written`. `git log` sobre `learning/sources.yaml`: primera
  línea `91e27cca 2026-09-09 fix(agents): cite every ISO standard by a fiche that answers`. `git log`
  sobre `evaluations/expected-behaviors.yaml evaluations/cases`: primera línea `acc2e137 2026-08-23
  refactor!: teams are flows, because none of them is a team`. `git status --porcelain`: única línea
  `?? agents/roles/system/technical-program-manager/learning/reports/`.

Limitaciones de esta corrida:

- No se pudo leer ninguna ficha de `iso.org` ni `pmi.org` de forma directa (403 en las seis URL
  intentadas); todo lo referido a esos dominios queda en registro **documentado**, corroborado por al
  menos dos consultas independientes cuando fue posible, y nunca en **verificado**.
- No se re-midió Node/npm por esta corrida: se toma el dato de la tarea (Node v24.21.0, npm 11.19.0)
  tal como se indicó, sin marcarlo verificado.
- No se investigó en profundidad la mención secundaria a una posible 5ª edición del *Standard for
  Portfolio Management*: sólo apareció en resúmenes de búsqueda, sin fuente primaria ni secundaria
  identificable que la sostenga con fecha o número ANSI. Queda en «Preguntas abiertas» y no se afirma.
- No se corrió `agent-propose`: no está entre lo que `learning/AUTOMATION.md` pide ejecutar en este
  ciclo, y el propio `make agent-learn` de esta corrida lo señala como paso posterior y separado.

## Posibles prácticas obsoletas

Ninguna. Los cuatro estándares base de `sources.yaml` siguen vigentes en la edición que ya citan
`sources.yaml` y `references/operating-model.md` (H1). Lo único que se mueve es que ISO 31000 tiene un
sucesor en desarrollo (H3) — no vuelve obsoleta a la edición 2018, que sigue siendo la única publicada.

## Recomendación

Para la propuesta mensual de `learning/proposals/`, sujeta a aprobación humana.

1. **Agregar `sources.yaml`: el nuevo estándar de PMI sobre IA (H2).** Candidata de entrada: `{name:
   PMI Standard for Artificial Intelligence in Portfolio, Program, and Project Management, url:
   "https://www.pmi.org/about/press-media/2026/pmi-publishes-worlds-first-global-standard-for-ai-in-project-work", tier: profession, topics: [ai governance, lifecycle, guardrails]}`. La razón para
   proponerlo y no sólo anotarlo: ya complementa formalmente los tres estándares base que este cargo
   cita (dicho por la propia fuente en H2) y el `SKILL.md` del cargo ya pide guardrails y rollout en su
   entrega mínima — hay una superficie de contrato con la que conecta. No se propone tocar `SKILL.md`
   ni `evaluations/`: ninguna regla ni caso depende de este estándar todavía, y la evaluación de si
   corresponde adoptar su vocabulario es la que sigue tras esta propuesta, no parte de ella.
2. **Actualizar `references/operating-model.md`, línea de ISO 31000 (H3).** Agregar el detalle de que
   hay una Edición 3 en borrador de comité (ISO/CD 31000, sin fecha de publicación), en vez de la
   mención genérica actual («indica futura revisión»). Es una precisión, no un cambio de fondo: la
   recomendación operativa de este cargo sobre riesgo no cambia mientras 2018 siga siendo la única
   edición publicada.
3. **No agregar ISO 21513 ni ISO 21508 Ed.2 todavía (H4).** Ninguna de las dos está publicada. Revisar
   en la corrida de 2026-10 si ya salieron; si `committee.iso.org/sites/tc258/home/projects.html`
   sigue mostrándolas «en desarrollo», no hay nada nuevo que registrar sobre ellas.
4. **H1 y H5 no requieren acción.** H1 confirma que las cuatro fuentes existentes siguen correctas tal
   como están citadas. H5 es una nota operativa: el contrato de este cargo cambió (2026-09-02) después
   del último veredicto de casos (2026-08-23), y ninguno de los cuatro hallazgos de fondo de esta
   corrida lo agrava ni lo cierra — queda para quien decida si corresponde adelantar una evaluación en
   vivo.

## Preguntas abiertas

1. **¿El nuevo estándar de PMI sobre IA (H2) tiene acreditación ANSI?** No se encontró mención en
   ninguna fuente consultada, a diferencia de la 5ª edición de Program Management. No se afirma que
   carezca de ella — sólo que esta corrida no lo pudo confirmar.
2. **¿Qué evidencia de adopción tiene el estándar de IA de PMI?** Recién publicado (2026-06-09, poco
   más de tres meses antes de esta corrida); ninguna fuente trae casos, retrospectivas ni crítica de la
   comunidad todavía. Revisar en una corrida futura, no antes: tres meses es poco tiempo para que exista
   evidencia de outcome sobre un estándar de esta escala.
3. **¿Existe una 5ª edición del PMI *Standard for Portfolio Management*?** Un resultado de búsqueda lo
   mencionó de pasada («ANSI mandated comment period... is closed») sin fuente primaria identificable;
   el ANSI Webstore sólo muestra la 4ª edición (2017, `ANSI/PMI 08-003-2017`). No se afirma ni se
   descarta — queda sin verificar.
4. **¿Cuándo se publica ISO/CD 31000 Edición 3?** Ninguna fuente consultada trae una fecha objetivo
   (H3). Revisar junto con H4 en la corrida de 2026-10.
5. **¿Corresponde adelantar una evaluación en vivo de este cargo dado que `SKILL.md` cambió
   (2026-09-02) después del último veredicto de casos (2026-08-23)?** (H5). No es una pregunta de la
   profesión de TPM sino del mecanismo compartido de `agent-evaluate`; se deja para quien consolide la
   propuesta mensual.
6. **Contexto de empresa**: este informe cubre la profesión, no una instalación. No evalúa
   `organization/roles/technical-program-manager.md` de ninguna instancia concreta — ese archivo no
   existe en este repositorio (es el molde, `mode: toolkit`).
