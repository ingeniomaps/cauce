---
agent: security-engineer
date: 2026-09-14
status: draft
propone: no
---

# Investigación semanal — 2026-09-14

<!-- Tres convenciones que el ciclo necesita y que nada más sostiene:

  · Etiquetá cada hallazgo H1, H2, … en el orden en que aparecen. «Evidencia» y «Recomendación» se
    refieren a ellos por esa clave, y la propuesta mensual la cita para decir de qué hallazgo sale
    un cambio. Sin etiqueta las tres secciones dejan de cruzarse y nada lo delata.

  · No renombres los títulos. «## Recomendación» se lee con un patrón exacto y es lo único que la
    propuesta consolida de cada informe: renombrarlo no da error, deja la propuesta vacía.

  · Contestá «propone» en el frontmatter, con «si» o con «no». Es «si» cuando el informe pide tocar
    algún archivo del cargo —SKILL.md, sources.yaml, references/, un caso— y «no» cuando lo hallado
    no cambia ningún contrato, aunque «Recomendación» explique largamente por qué. Un «no» se
    mergea sin revisión humana: no hay nada que decidir. Ante la duda va «si», que sólo cuesta una
    mirada.

Este comentario vive fuera de toda sección a propósito — dentro de «Recomendación» viajaría a cada
propuesta consolidada. -->

## Fuentes consultadas

Entorno de esta corrida, dado por el job y no vuelto a comprobar (sin shell propio para eso, según indica
quien lanzó esta investigación): **Node v24.20.0 y npm 11.19.0** — exactamente los mismos números que
declaraba el job del informe anterior (2026-09-07). Es la primera semana en tres informes seguidos en que
el entorno no avanzó de patch.

Inventario local, contra el informe anterior (`learning/reports/2026-09-07.md`) y contra
`learning/HISTORY.md` para no repetir como hallazgo lo ya aprobado y aplicado:

- **Git ahora trae el historial completo en este checkout**: `git log --oneline | wc -l` devuelve **1883**
  commits, hasta `1715d5e4` (el primero del repositorio). Los dos informes anteriores señalaron
  repetidamente que sólo veían un commit —el merge que trajo el checkout— y que eso no alcanzaba para
  atribuir intención o fecha a un cambio local. Ver H7.
- `package.json` — `@ingeniomaps/cauce@0.89.0` (era `0.68.0` la semana pasada: 21 versiones). `files` sigue
  con los mismos tres patrones negativos, sin cambios. `engines.node: ">=24"` y
  `packageManager: npm@11.16.0`, ambos sin cambios en cuatro informes seguidos.
- `CHANGELOG.md` — 21 entradas nuevas entre `[0.69.0] - 2026-09-07` y `[0.89.0] - 2026-09-14`, todas
  completas, leídas enteras. La mayoría son cambios de proceso del propio motor (colas, `wip/`, `done/`,
  `RECURRING.md`); el subconjunto de contenido de seguridad se detalla en H8-H10.
- `.github/workflows/*.yml` (los seis) — completos. Los pines de `actions/checkout`
  (`3d3c42e5aac5ba805825da76410c181273ba90b1`, `v7.0.1`) y `actions/setup-node`
  (`820762786026740c76f36085b0efc47a31fe5020`, `v7.0.0`) siguen siendo los mismos SHA en los seis archivos.
  `agent-learning.yml:225` sigue fijando `@anthropic-ai/claude-code@2.1.233` (la línea se movió de 218 a
  225 por el crecimiento del archivo, el pin no cambió). Cambio nuevo: `agent-learning.yml`, `ci.yml` y
  `sign-proposal.yml` ahora usan `fetch-depth: 0` (antes, checkout superficial); `open-pr.yml` sigue en
  `fetch-depth: 1` y `release-pr.yml` trae un comentario explícito de por qué no usa `fetch-depth: 0`. Ver
  H7.
- `engine/hooks/shell.js`, `engine/hooks/ops-config.js`, `engine/hooks/secrets-shell.js`,
  `engine/hooks/files.js`, `engine/hooks/run.js` — leídos directamente para verificar lo que el CHANGELOG
  describe entre `[0.80.0]` y `[0.87.0]`: los guards `secrets-read`, `secrets-shell`, `ops-config` y
  `ops-config-shell` existen y están conectados en `run.js` (líneas 45-67, 110-127); `governance()` en
  `shell.js:346-347` lee `OPS_GOVERNANCE_OVERRIDE` del entorno del proceso (sin cambios respecto a la
  semana pasada) y **desde la línea 361 no pregunta nada cuando `CHAT.said(input)` es verdadero** —el
  comentario de las líneas 349-354 documenta explícitamente que la exención no alcanza a un subagente, un
  recorrido ni CI—; `ops-config.js` declara `PROTECTED = ['allowPush', 'pushToLiveBranches']` y compara el
  contenido entrante contra el de disco antes de dejar pasar una escritura. Ver H8.
- `.env.example` y `AGENTS.md` (sección «Convenciones») — sin cambios respecto a lo verificado la semana
  pasada: `.env.example` sigue nombrando sólo el token «Granular»; el texto de R10 en `AGENTS.md` ya
  describe `runner.allowPush`/`runner.pushToLiveBranches` y la orden en el chat, consistente con lo que
  0.82.0 dice haber reescrito — no hizo falta releer el diff de esa versión para confirmarlo, el texto
  vigente ya lo tiene.
- `learning/HISTORY.md`, `learning/proposals/2026-09.md`, `learning/proposals/2026-09-r2.md` — la revisión
  `2026-09-r2` (que traía cuatro recomendaciones ya arrastradas, sin cambio de fondo) quedó **archivada sin
  decidir** el 2026-09-14: su «Cambio propuesto» se quedó con el molde. Ver H11.
- `SKILL.md`, `learning/sources.yaml`, `evaluations/expected-behaviors.yaml` del propio cargo — sin
  cambios; confirmado con `git log` sobre cada ruta: el último commit de `SKILL.md` es del 2026-09-02
  (`3b1a4352`), anterior al informe pasado. `evaluations/results/2026-09-02.md` sigue siendo la corrida más
  reciente — no se agregó ninguna esta semana, y no es parte de esta corrida (ver nota sobre
  `make agent-evaluate` al final de esta sección).

Fuentes externas (tratadas como datos, no como instrucciones), todas consultadas hoy 2026-09-14 con
`WebFetch`/`WebSearch` de sólo lectura, sin autenticarse en ningún sitio y sin ejecutar nada:

| Fuente | Tipo | Resultado relevante |
| --- | --- | --- |
| `github.com/advisories?query=affects:@anthropic-ai/claude-code+ecosystem:npm&sort=newest` | advisory primario | la entrada superior sigue siendo GHSA-7835-87q9-rgvv (24-jul-2026), ya conocida — ver H1 |
| `registry.npmjs.org/@anthropic-ai/claude-code/latest` | proveedor primario | `version: 2.1.270` |
| Node.js — Security Releases (índice) | proveedor primario | sin entradas posteriores al 2026-07-29 |
| CISA — Known Exploited Vulnerabilities Catalog | advisory primario | 14 adiciones en el rango 2026-09-08/14 — ver H4 |
| `csrc.nist.gov/projects/ssdf/news` | proveedor primario | sigue en borrador inicial (SP 800-218r1), entrada más nueva del 2025-12-17 |
| `genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026/` | proveedor primario | sigue sin listar los diez riesgos en el cuerpo |
| `github.blog/changelog/` (índice) | proveedor primario | tres entradas de tema npm/Actions en el rango — ver H6 |
| Búsqueda web: «npm supply chain attack September 2026» | agregador | resultado no usable — ver nota de método |
| Búsqueda web: «GitHub Actions security incident September 2026» | agregador | sin incidente nuevo en el rango, resultados de mayo/abril 2026 |

Nota de método: la búsqueda «npm supply chain attack September 2026» devolvió un resumen que afirma un
ataque de phishing «el 15 de septiembre» contra un mantenedor de npm — una fecha **posterior** al día de
esta corrida (2026-09-14) — mezclado con la descripción del gusano Shai-Hulud, que los otros resultados
de la misma búsqueda (Aikido, Unit42, Red Hat) sitúan en incidentes ya conocidos de informes anteriores de
este cargo (keyv, 2026-08-04) y no como novedad de esta semana. No se pudo corroborar contra una fuente
primaria con fecha verificable, y la fecha futura es en sí misma una señal de que el resumen mezcló
material de distintas épocas. No se trata como hallazgo — ver Preguntas abiertas.

Sobre el cierre de esta corrida: la instrucción de quien lanzó esta investigación fue completar
únicamente este informe; no se ejecutó `make agent-learn` (el informe ya estaba creado) ni
`make agent-evaluate` que `learning/AUTOMATION.md` pide como paso final — ese comando escribiría en
`evaluations/results/`, fuera del único archivo autorizado para esta corrida.

## Hallazgos

### H1 — Sin advisories nuevas para `@anthropic-ai/claude-code`; la brecha entre el pin de CI y la última versión publicada sigue creciendo

- **Estado**: continúa H1 de los dos informes anteriores, sin novedad de fondo.
- **Verificado hoy**: con la consulta ordenada por `sort=newest`, la entrada superior de la Advisory
  Database sigue siendo GHSA-7835-87q9-rgvv / CVE-2026-55607 (parcheada en 2.1.163) — la misma de las tres
  semanas anteriores.
- **Documentado**: `registry.npmjs.org/@anthropic-ai/claude-code/latest` devuelve `2.1.270` — sube desde
  `2.1.263` (informe anterior) y `2.1.258` (el previo a ése). El pin de `agent-learning.yml:225` sigue en
  `2.1.233`, por delante de `2.1.163` (la versión parcheada más alta de las tres advisories abiertas hace
  dos semanas) y cada vez más lejos de la última publicada. La brecha entre pin y última versión es sólo
  diferencia de versión, no evidencia de vulnerabilidad sin parchear: no se encontró ninguna advisory con
  parche por encima de `2.1.233`.
- **Lo que esto no establece**: las 25 advisories restantes de las 28 (ver informe 2026-09-02) siguen sin
  abrirse una por una — sigue siendo extrapolación, no verificación completa (pregunta abierta 1,
  arrastrada por tercera semana).
- **Confianza**: **alta** en que no apareció ninguna advisory nueva; **media** en que las 25 restantes no
  superen el pin, sin cambios.

### H2 — Node.js: sin nueva security release desde el 2026-07-29

- **Estado**: continúa H2 de los dos informes anteriores, sin novedad.
- **Verificado hoy**: el índice de security releases de Node.js sigue sin listar ninguna entrada posterior
  al 2026-07-29.
- **Local**: `package.json` sigue con `"node": ">=24"`, sin piso de parche.
- **Confianza**: **alta**.

### H3 — El entorno de la corrida no avanzó esta semana; `packageManager` del repo sigue fijo en npm 11.16.0

- **Estado**: rompe el patrón de los dos informes anteriores, que registraron avance de patch cada semana.
  El entorno de esta corrida (Node v24.20.0, npm 11.19.0, dado por el job) es **idéntico** al del informe
  del 2026-09-07.
- **Local**: `package.json` — `packageManager: npm@11.16.0`, sin cambios en cuatro informes seguidos. La
  brecha entre lo declarado y lo que el entorno real usa no creció esta semana, a diferencia de las dos
  anteriores — se mantiene en el mismo tamaño que la semana pasada.
- **Confianza**: **alta** en el hecho local; el número de entorno se toma dado según la instrucción de
  esta corrida, no se comprobó con un comando propio.

### H4 — CISA KEV: catorce adiciones en el rango; ninguna aplica al stack declarado de Cauce

- **Estado**: continúa el patrón de H4 de los dos informes anteriores, con un volumen mayor de adiciones.
- **Documentado**: catorce adiciones en el rango 2026-09-08/2026-09-14 (ConnectWise ScreenConnect, JFrog
  Artifactory ×2, GitLab CE/EE, MikroTik RouterOS ×2, Citrix NetScaler, Fortinet, Chromium V8, Cisco
  Firewall Management Center, Adobe Commerce/Magento, Microsoft Windows ×2, N-able N-central). Ninguna es
  Node.js, npm ni GitHub Actions, y Cauce sigue sin bloque `dependencies` en `package.json` ni usa
  Artifactory, NetScaler, Fortinet, RouterOS, Firewall Management Center, Magento, Windows Update Stack ni
  N-central en su propio pipeline.
- **Hipótesis, no comprobable acá**: igual que CVE-2026-85046 la semana pasada, CVE-2026-87491 (Chromium
  V8, escritura fuera de límites) repite la misma pregunta sin resolver — si el V8 embebido en el Node.js
  que corre este toolkit comparte el rango afectado. Sigue sin sostener ninguna prioridad ni acción, sigue
  nombrada y no descartada (pregunta abierta, arrastrada).
- **Confianza**: **alta** en la catalogación (fechas, CVE, productos, leídos del catálogo); **hipótesis**
  en la aplicabilidad de la entrada de V8 al runtime de este proyecto.

### H5 — SSDF v1.2 sigue en borrador

- **Estado**: continúa H5 de los dos informes anteriores, sin cambios.
- **Verificado hoy**: `csrc.nist.gov/projects/ssdf/news` sigue mostrando la entrada de 2025-12-17 como la
  más reciente, anunciando el borrador inicial de SP 800-218r1, sin fecha de versión final.
- **Lectura**: `learning/sources.yaml` sigue citando `csrc.nist.gov/pubs/sp/800/218/final`, que resuelve a
  la v1.1 vigente — sigue siendo la cita correcta.
- **Confianza**: **alta**.

### H6 — GitHub Changelog: tres entradas de npm/Actions en el rango, ninguna exige acción de Cauce

- **Estado**: hallazgo nuevo — los dos informes anteriores no habían encontrado entradas del Changelog en
  el rango de la semana.
- **Documentado**, leído del índice hoy: «Multiple trusted publishing configurations for npm»
  (2026-09-03) permite declarar más de un flujo de publicación confiable por paquete OIDC — Cauce publica
  con un único flujo (`release.yml`, un solo paquete, un solo trigger por tag), así que no hay un segundo
  flujo que declarar hoy; «npm extends recovery-code security holds to all accounts» (2026-09-09) es
  protección de cuenta de mantenedor de npm en general, sin una acción concreta que dependa de este
  repositorio; «Control GitHub Actions cache access with cache-mode» (2026-09-10) es un control de
  aislamiento para `actions/cache` — **verificado que ninguno de los seis workflows de Cauce usa
  `actions/cache`** (`grep -r actions/cache .github/workflows` sin resultados), así que no aplica.
- **Confianza**: **alta** en el contenido y fecha de las tres entradas (leídas del índice hoy); **alta** en
  que ninguna exige un cambio en este repositorio hoy.

### H7 — Local: el checkout de esta corrida trae el historial completo, resolviendo la limitación que los dos informes anteriores señalaron repetidamente

- **Estado**: hallazgo nuevo, local. Responde parcialmente la pregunta abierta 2 del informe 2026-09-07 y
  la pregunta abierta 2 del informe 2026-09-02.
- **Verificado hoy**: `git log --oneline | wc -l` devuelve **1883**, desde `1715d5e4` («build: add toolkit
  manifest, make targets and ignore rules») hasta el merge más reciente. Los dos informes anteriores
  registraron explícitamente «un solo commit visible en este checkout» como límite para atribuir intención
  o fecha a un cambio local, y esta corrida no tiene esa limitación: por ejemplo, `git log --oneline --
  agents/roles/system/security-engineer/SKILL.md` devuelve el historial completo de ese archivo, con
  fechas, y permitió confirmar en esta misma corrida (ver «Fuentes consultadas») que el contrato del cargo
  no cambió desde el 2026-09-02 sin depender de lo que dice el informe anterior.
- **Verificado hoy, leyendo el CHANGELOG**: `[0.72.0] - 2026-09-09` describe el motivo — `agent evaluate`
  necesitaba la fecha de cambio de un `SKILL.md` vía `git log`, y un checkout superficial le atribuye el
  árbol entero al único commit visible; el arreglo agregó `fetch-depth: 0` al job que evalúa. Consistente
  con lo observado: `agent-learning.yml:213` trae `fetch-depth: 0` en el job `research` (línea 78 en
  adelante), que es el que corre esta misma investigación.
- **Lo que esto no establece**: no se usó el historial completo para reabrir y contestar retroactivamente
  las preguntas abiertas de fecha/intención de los dos informes anteriores (por ejemplo, cuándo y por qué
  se aplicaron las catorce versiones de la semana 09-07) — eso queda para el próximo informe, ver
  Recomendación 4.
- **Confianza**: **alta** — verificado corriendo el comando en esta misma máquina y leyendo el código del
  workflow que la produce.

### H8 — Local: ciclo de endurecimiento de autorización y credenciales entre `[0.80.0]` y `[0.87.0]`, verificado en el código vigente

- **Estado**: hallazgo nuevo, local. Es el más relevante de este informe para el contrato del cargo, porque
  toca exactamente los ejes que `SKILL.md` nombra —autorización, secretos, automatización con
  credenciales— dentro del propio producto que este cargo vigila.
- **Verificado hoy leyendo `engine/hooks/run.js`, `shell.js`, `ops-config.js`, `secrets-shell.js` y
  `files.js`** (no se infiere sólo del CHANGELOG):
  - Dos guards nuevos leen credenciales: `secrets-read` (Read/`read_file`, agregado en 0.80.0) y
    `secrets-shell` (`cat`, `grep`, `source`, redirecciones, agregado en 0.81.0), conectados en
    `run.js:45-67,110-127`.
  - Dos guards nuevos protegen sólo dos llaves de `ops.config.json` (`runner.allowPush` y
    `runner.pushToLiveBranches`, las que deciden qué `git push` se publica): `ops-config.js:20` declara
    `PROTECTED = ['allowPush', 'pushToLiveBranches']` y compara contenido entrante contra disco antes de
    dejar pasar la escritura; `ops-config-shell` frena toda escritura por shell sobre el archivo entero,
    porque un comando no dice con qué va a quedar (0.83.0).
  - `allowPush: true` dejó de alcanzar a `main`/`master`/la rama por defecto y a un subagente sin
    `runner.pushToLiveBranches` explícito (0.82.0) — el mecanismo que R10 describe en `AGENTS.md` ya está
    escrito así, confirmado en «Fuentes consultadas».
  - `governance()` en `shell.js:346-383` deja de preguntar **sólo** cuando `CHAT.said(input)` es verdadero
    —hay una persona nombrando la ruta en el chat—, y el comentario de las líneas 349-354 dice
    explícitamente que esa exención no alcanza a un subagente, un recorrido de Cauce ni a CI (0.85.0). Es
    la misma distinción que `SKILL.md` § Reglas de construcción pide desde la propuesta 2026-08 de este
    cargo: «un proceso que lee entrada no confiable, decide y actúa con las credenciales del pipeline se
    compromete dirigiéndolo, no robándole el token» — acá el guard la aplica al propio commit de
    gobernanza del toolkit.
  - Dos rastros de auditoría nuevos, sólo locales y gitignoreados: `planning/.push-log` (una línea por push
    aprobado, con fecha, remoto, rama, vía y sesión — 0.83.0) y `planning/.grant-log` (una línea por
    concesión en el chat, sin el texto de lo que se escribió — 0.86.0).
- **Lo que esto no establece**: no se corrió un caso propio contra estos guards esta semana (por ejemplo,
  intentar escribir `runner.allowPush` por `sed -i` y observar el bloqueo) — lo verificado es que el código
  que implementa el mecanismo está en el árbol y coincide con lo que el CHANGELOG describe, no una
  ejecución que lo ejerza.
- **Confianza**: **alta** en que el código existe y coincide con la descripción — verificado por lectura
  directa de las cinco fuentes citadas; **sin verificar por ejecución propia** el comportamiento en runtime
  esta semana.

### H9 — Local: `[0.87.0]` corrige que el escaneo de credenciales sólo analizaba las primeras cuarenta variables por servicio

- **Estado**: hallazgo nuevo, local — bug de seguridad en el propio guard de secretos del toolkit,
  corregido esta semana.
- **Documentado, leído del CHANGELOG**: hasta 0.86.0, el aviso de credenciales sin dueño cortaba el
  **análisis** —no sólo el listado— en cuarenta variables por servicio; medido contra una instancia real
  con 61 variables, dos credenciales sin dueño en las líneas 43 y 60 quedaban completamente fuera de
  cualquier aviso. 0.87.0 mueve el corte para que recaiga sólo sobre lo que se **muestra** (`scan` sigue
  listando cuarenta y dice cuántas más hay), no sobre lo que se **analiza**.
- **Lo que esto no establece**: no se verificó corriendo `node engine/cli/ops.js check` contra un servicio
  con más de cuarenta variables en esta corrida — se toma el hecho tal como lo documenta el CHANGELOG, sin
  ejecución propia.
- **Confianza**: **documentado** (CHANGELOG, sin ejecución propia esta semana); el hecho de que el
  CHANGELOG describe un defecto de seguridad real, con número concreto (dos credenciales invisibles de
  61), es consistente con la forma en que los dos informes anteriores encontraron y verificaron defectos
  similares en `engine/hooks/shell.js`.

### H10 — Local: confirmación de que R9 y el código vigente coinciden en el caso `CI=true` / borrado de `node_modules`

- **Estado**: verificación de continuidad, no hallazgo nuevo — el texto de R9 en
  `template/planning/rules/system/commits.md` ya cita este caso («Poner `CI=true` para que un gestor de
  paquetes dejara de confirmar antes de purgar borró el árbol de dependencias de un proyecto real»); esta
  semana se confirmó contra el CHANGELOG y el código actual, no sólo contra la regla.
- **Verificado hoy leyendo el CHANGELOG**: `[0.74.0]` introdujo `CI=true` en la copia que usan los gates
  para evitar que pnpm pidiera confirmación antes de reinstalar; `[0.75.0]`, la misma semana, lo revirtió
  al medir que esa confirmación era lo único que protegía el árbol enlazado de `node_modules` del proyecto
  real, y la reemplazó por `verify-deps-before-run=false` —que le dice a pnpm que no sincronice nada antes
  de correr, sin desarmar ninguna confirmación—.
- **Confianza**: **alta** — verificado leyendo las dos entradas del CHANGELOG, consistente con el texto de
  R9 que ya cita el caso.

### H11 — Local, sin acción: la revisión mensual de este cargo (`2026-09-r2`) quedó archivada sin decidir

- **Estado**: hecho registrado en `learning/HISTORY.md`, sin acción pendiente para este informe.
- **Verificado hoy**: `learning/proposals/2026-09-r2.md` tiene `status: archived`; su sección «Cambio
  propuesto» quedó con el texto del molde («Una revisión suele **no** ser aditiva…») sin completar.
  `learning/HISTORY.md`, fila 2026-09-14, registra que se archivó «sin decidir: el documento quedó con el
  molde». `[0.88.0] - 2026-09-14` del CHANGELOG describe el defecto que esto destapó —una revisión con el
  molde intacto se podía sellar como aplicada sin que nadie hubiera decidido nada— y dice que pasó con
  siete cargos el 2026-09-14; el arreglo hace que el molde de una revisión ya no pueda sellarse.
- **Lo que esto no establece**: no hay señal de que las cuatro recomendaciones arrastradas dentro de esa
  revisión (las mismas de H1-H3, sin cambios de fondo) se hayan perdido — siguen vivas en este informe y
  en el anterior, con su propio seguimiento.
- **Confianza**: **alta** — verificado leyendo los dos documentos y la fila de `HISTORY.md`.

## Evidencia

Todo lo local fue verificado por lectura y por un comando de git en esta máquina, sin tocar sistemas
remotos, sin credenciales y sin escanear nada. No se leyó ningún valor de `.env` (no existe en este árbol
de trabajo). Las consultas externas fueron lecturas de páginas públicas y un endpoint de sólo lectura del
registro de npm, hechas directamente con `WebFetch`/`WebSearch`, sin autenticarse en ningún sitio y sin
efecto sobre ningún sistema de terceros.

| # | Evidencia | Tipo |
| --- | --- | --- |
| H1 | `github.com/advisories?query=affects:@anthropic-ai/claude-code+ecosystem:npm&sort=newest` (fetch 2026-09-14); `registry.npmjs.org/@anthropic-ai/claude-code/latest` (fetch 2026-09-14); `.github/workflows/agent-learning.yml:225` | externa primaria + local |
| H2 | nodejs.org/en/blog/vulnerability/ (fetch 2026-09-14); `package.json` (`engines.node`) | externa primaria + local |
| H3 | `package.json` (`packageManager`); versión de entorno dada por el job | local |
| H4 | cisa.gov/known-exploited-vulnerabilities-catalog (fetch 2026-09-14); `package.json` sin bloque `dependencies` | externa primaria + local |
| H5 | csrc.nist.gov/projects/ssdf/news (fetch 2026-09-14); `learning/sources.yaml` | externa primaria |
| H6 | github.blog/changelog/ (fetch 2026-09-14); `grep -r actions/cache .github/workflows` (sin resultados) | externa primaria + local |
| H7 | `git log --oneline \| wc -l` (comando propio, 2026-09-14); `CHANGELOG.md` entrada `[0.72.0]`; `.github/workflows/agent-learning.yml:78,213` | local |
| H8 | `CHANGELOG.md` entradas `[0.80.0]`-`[0.87.0]`; `engine/hooks/run.js:45-67,110-127`; `engine/hooks/shell.js:346-383`; `engine/hooks/ops-config.js:1-30`; `AGENTS.md` sección «Convenciones» | local |
| H9 | `CHANGELOG.md` entrada `[0.87.0]` | local (documentado, sin ejecución propia) |
| H10 | `CHANGELOG.md` entradas `[0.74.0]`, `[0.75.0]`; `template/planning/rules/system/commits.md` (texto de R9) | local |
| H11 | `learning/proposals/2026-09-r2.md`; `learning/HISTORY.md` fila 2026-09-14; `CHANGELOG.md` entrada `[0.88.0]` | local |

## Posibles prácticas obsoletas

1. **`engines.node: ">=24"` sin piso de parche.** Sin cambios desde los dos informes anteriores; ver H2.
   (Confianza: **alta**.)
2. **`packageManager: npm@11.16.0` por detrás del entorno real (11.19.0).** La brecha no creció esta
   semana —a diferencia de las dos anteriores, ver H3— pero sigue sin cerrarse. (Confianza: **alta** en el
   hecho local; **media** en si conviene saltar directo a npm 12, no revisado esta semana.)
3. **Vigilar, no cambiar todavía**: `learning/sources.yaml` sigue apuntando a SP 800-218 «final» (v1.1).
   Correcto mientras v1.2 siga en borrador — ver H5.

## Recomendación

Nada de lo que sigue se aplicó: este informe no toca dependencias, controles, secretos, código, `SKILL.md`
ni planificación. Tampoco se corrió `make agent-evaluate` — ver la nota de cierre en «Fuentes consultadas».

**Prioridad media**

1. Antes de apoyar cualquier decisión en «el pin de `agent-learning.yml` no tiene vulnerabilidades
   conocidas», abrir las 25 advisories restantes de `@anthropic-ai/claude-code` una por una y confirmar que
   ninguna tiene versión parcheada por encima de `2.1.233`. Arrastrada sin cambios desde el informe
   2026-09-02 (H1).

**Prioridad baja — arrastradas de informes anteriores, sin cambios**

2. Subir el piso de `engines.node` a la línea parcheada vigente en vez de `>=24` abierto (H2).
3. Evaluar mover `packageManager` a una versión de npm más cercana al entorno real (H3).

**Nota de método, sin prioridad — para el próximo informe**

4. Con el historial completo ya disponible en el checkout (H7), reabrir la pregunta abierta 2 del informe
   2026-09-07 —cuándo y por qué se aplicaron, versión por versión, las catorce entradas de esa semana— y
   la pregunta equivalente del informe 2026-09-02 sobre `package.json` → `files`; ahora tienen con qué
   contestarse sin depender de un solo commit visible.
5. Repetir la consulta a la Advisory Database con una segunda forma (paginando explícitamente) para
   resolver la discrepancia de conteo (28 según la página, 25 enumeradas) que el informe 2026-09-07 dejó
   sin cerrar — no cambia la conclusión de H1 pero deja una duda barata de cerrar.

**Sobre el contrato del cargo**: nada de esto exige cambiar `SKILL.md`, `learning/sources.yaml` ni
`evaluations/expected-behaviors.yaml`. Ninguna recomendación de informes anteriores queda pendiente de
retirar esta semana.

## Preguntas abiertas

1. **¿Las 25 advisories restantes de `@anthropic-ai/claude-code` (no abiertas individualmente) tienen
   todas versión parcheada ≤2.1.163?** Arrastrada de los dos informes anteriores, sin resolver esta semana.
   Ver H1 y recomendación 1.
2. **¿El V8 embebido en el Node.js que ejecuta este toolkit comparte el rango afectado por CVE-2026-87491
   (o por CVE-2026-85046, de la semana pasada)?** Ver H4. Sigue en hipótesis por el mismo motivo que la
   semana pasada: el vector público de cada una no tiene equivalente evidente en un CLI que no renderiza
   HTML ni corre un navegador no confiable.
3. **¿Es real el ataque de phishing contra un mantenedor de npm que un resumen de búsqueda ubicó el
   «15 de septiembre» — un día posterior a esta corrida?** No se pudo corroborar contra una fuente primaria
   con fecha verificable; el propio resumen mezcla ese dato con la descripción de un incidente (Shai-Hulud)
   que otras fuentes de la misma búsqueda sitúan en un período distinto. Se resolvería con una búsqueda
   angosta a una fuente fechable —un aviso de npm, un post de un investigador con fecha propia— en el
   próximo informe, sin necesidad de tocar ningún sistema. Ver nota de método en «Fuentes consultadas».
4. **¿Cuándo y por qué se aplicaron, versión por versión, las 21 entradas de esta semana
   (`[0.69.0]`-`[0.89.0]`)?** El contenido de cada una está verificado leyendo el CHANGELOG y, para el
   subconjunto de H8-H10, el código; la secuencia temporal real y quién decidió cada cambio no se
   reconstruyó esta semana pese a que el historial completo ya está disponible (H7) — queda para el
   próximo informe (recomendación 4).
