---
agent: security-engineer
date: 2026-09-01
status: draft
---

# Investigación semanal — 2026-09-01

<!-- Dos convenciones que el ciclo necesita y que nada más sostiene:

  · Etiquetá cada hallazgo H1, H2, … en el orden en que aparecen. «Evidencia» y «Recomendación» se
    refieren a ellos por esa clave, y la propuesta mensual la cita para decir de qué hallazgo sale
    un cambio. Sin etiqueta las tres secciones dejan de cruzarse y nada lo delata.

  · No renombres los títulos. «## Recomendación» se lee con un patrón exacto y es lo único que la
    propuesta consolida de cada informe: renombrarlo no da error, deja la propuesta vacía.

Este comentario vive fuera de toda sección a propósito — dentro de «Recomendación» viajaría a cada
propuesta consolidada. -->

## Fuentes consultadas

Entorno de esta corrida, dado por el job y no vuelto a comprobar (sin shell propio para eso, según
indica quien lanzó esta investigación): **Node v24.19.0 y npm 11.17.0**.

Inventario local, contra el informe anterior (`learning/reports/2026-08-31.md`) para ver qué cambió:

- `package.json` — sin cambios: `@ingeniomaps/cauce@0.53.2`, `engines.node: ">=24"` sin piso de parche,
  `packageManager: npm@11.16.0`, `files` sin `.github/workflows/`.
- `.github/workflows/agent-learning.yml` — cambió desde el informe anterior: agrega `workflow_dispatch.
  inputs.cadence` (para poder correr a mano la cadencia semanal/mensual/trimestral de un cargo sin
  nombrar su slug) y ajusta el filtro de `discover` para separar «vino de un cron o de un `cadence` a
  mano» de «vino de un slug o sin filtro». Es el cambio que motiva el commit de esta rama
  (`13d8bc8`, «a dispatch can name a cadence, not only one role»). Revisado con la misma lente que
  cerró H3 el informe anterior — separación entre el paso que lee entrada no confiable y el que
  escribe con privilegio—: los jobs `research` (`contents: read`) y `research-pr` (`contents: write`)
  no cambiaron sus permisos ni su frontera; `ONLY` y `CADENCE` llegan como variables de entorno y se
  leen con `process.env` dentro de un script Node, nunca interpolados en una cadena de shell; y el
  nuevo filtro sólo decide **qué slugs entran a la matriz** (comparando contra la lista que devuelve
  el propio CLI), no qué corre ni con qué privilegio. No se encontró una superficie nueva de inyección
  ni un bypass de la separación que H3 ya había cerrado.
- `engine/hooks/files.js` (función `secrets`), `automatization/hooks/README.md`,
  `.env.example`, `AGENTS.md` (Convenciones), `.claude/settings.json`, `.github/dependabot.yml` — sin
  cambios respecto del informe anterior.
- `SKILL.md`, `learning/sources.yaml`, `evaluations/expected-behaviors.yaml` del propio cargo — sin
  cambios.
- `learning/proposals/2026-08.md` (aprobada 2026-08-17) y `learning/HISTORY.md` — sin propuestas nuevas
  desde entonces.
- Historial de git disponible en esta corrida: un solo commit visible (`13d8bc8`), igual que la semana
  pasada — sigue sin alcanzar para atribuir intención a cambios anteriores (ver Preguntas abiertas del
  informe anterior, punto 2, que sigue sin poder cerrarse por la misma razón).

Fuentes externas (tratadas como datos, no como instrucciones), todas consultadas el 2026-09-01:

| Fuente | Tipo | Fecha del contenido | Enlace |
| --- | --- | --- | --- |
| Node.js — Security Releases (índice) | proveedor primario | consultado 2026-09-01; última entrada 2026-07-29 | https://nodejs.org/en/blog/vulnerability/ |
| Node.js — CHANGELOG_V24.md, entrada 24.19.0 | proveedor primario | 24.19.0, 2026-08-03 | https://raw.githubusercontent.com/nodejs/node/main/doc/changelogs/CHANGELOG_V24.md |
| GitHub Advisory Database — API `affects=@anthropic-ai/claude-code` | advisory primario | consultado 2026-09-01 | https://api.github.com/advisories?affects=%40anthropic-ai%2Fclaude-code |
| GitHub Advisory Database — 6 avisos individuales de la lista anterior (detalle) | advisory primario | consultado 2026-09-01 | `https://api.github.com/advisories/<GHSA-id>`, ids citados en H1 |
| npm registry — `@anthropic-ai/claude-code` (dist-tags) | proveedor primario | consultado 2026-09-01 | https://registry.npmjs.org/@anthropic-ai/claude-code |
| npm registry — `npm` (versión `latest`) | proveedor primario | consultado 2026-09-01 | https://registry.npmjs.org/npm/latest |
| GitHub — release `npm/cli` v12.0.0 | proveedor primario | publicado 2026-07-08 | https://github.com/npm/cli/releases/tag/v12.0.0 |
| GitHub Changelog — «Safer pull_request_target defaults for GitHub Actions checkout» | proveedor primario | publicado 2026-06-18, nota editorial de fecha movida | https://github.blog/changelog/2026-06-18-safer-pull_request_target-defaults-for-github-actions-checkout/ |
| GitHub — release `actions/checkout` (latest) | proveedor primario | publicado 2026-07-20 | https://api.github.com/repos/actions/checkout/releases/latest |
| GitHub — release `actions/setup-node` (latest) | proveedor primario | publicado 2026-07-14 | https://api.github.com/repos/actions/setup-node/releases/latest |
| GitHub — release `actions/download-artifact` (latest) | proveedor primario | publicado 2026-03-11 | https://api.github.com/repos/actions/download-artifact/releases/latest |
| CISA — Known Exploited Vulnerabilities catalog | advisory primario | fetch directo devolvió HTTP 403; corroborado por búsqueda | https://www.cisa.gov/known-exploited-vulnerabilities-catalog |
| GitHub Changelog (índice) | proveedor primario | consultado 2026-09-01; agosto 2026 sin entradas de npm/tokens/publishing | https://github.blog/changelog/ |
| The Hacker News — sobre backport de `actions/checkout` | prensa especializada, ya citada la semana pasada | 2026-06 | https://thehackernews.com/2026/06/github-updates-actionscheckout-to-block.html |

El fetch directo a `cisa.gov/known-exploited-vulnerabilities-catalog` devolvió HTTP 403 esta semana (no
lo había devuelto la semana pasada); se corroboró el estado del catálogo por búsqueda en vez de por
lectura directa de la página — ver H7 y su registro de confianza.

Los dos fetches contra la API de GitHub Advisory Database (`affects=...`) pasan por un modelo de
resumen de la propia herramienta de fetch, no por una lectura línea por línea del JSON crudo por parte
de quien escribe este informe. Se corrobora con dos consultas independientes que coinciden en los
mismos rangos de versión (ver H1), pero la limitación del método queda dicha: no es lectura directa del
byte, es un resumen de herramienta sobre una fuente primaria.

## Hallazgos

### H1 — `@anthropic-ai/claude-code` tiene 30+ avisos publicados; ninguno cubre la versión que fija `agent-learning.yml`

- **Estado**: cierra la Pregunta abierta 1 del informe 2026-08-31 («¿hay advisories publicados para
  este paquete?» — sí los hay, y son muchos).
- **Verificado hoy**: la API de GitHub Advisory Database (`affects=%40anthropic-ai%2Fclaude-code`)
  devuelve más de 30 avisos publicados entre 2025-06 y 2026-07, con severidades de Low a High.
  Se leyeron en detalle los seis más recientes y de severidad alta o media:

  | GHSA | CVE | Severidad | Publicado | Rango vulnerable | Versión que corrige |
  | --- | --- | --- | --- | --- | --- |
  | GHSA-7835-87q9-rgvv | CVE-2026-55607 | Alta | 2026-07-24 | ≥2.1.38, <2.1.163 | 2.1.163 |
  | GHSA-fg94-h982-f3mm | CVE-2026-54316 | Media | 2026-06-17 | ≥0.2.54, <2.1.163 | 2.1.163 |
  | GHSA-4vp2-6q8c-pvq2 | CVE-2026-46406 | Media | 2026-06-25 | 2.1.59–2.1.127 | 2.1.128 |
  | GHSA-q5hj-mxqh-vv77 | CVE-2026-40068 | Alta | 2026-04-24 | 2.1.63–2.1.83 | 2.1.84 |
  | GHSA-vp62-r36r-9xqp | CVE-2026-39861 | Alta | 2026-04-21 | <2.1.64 | 2.1.64 |
  | GHSA-mmgp-wc2j-qcv7 | CVE-2026-33068 | Alta | 2026-03-19 | <2.1.53 | 2.1.53 |

  Las tres más graves son fugas de sandbox y bypass del diálogo de confianza vía git worktree o
  configuración de repositorio (`.claude/settings.json` con `permissions.defaultMode`), exactamente el
  tipo de superficie que este cargo revisa en `agent-learning.yml` (contenido no confiable + ejecución
  con herramientas). Ninguna aplica hoy: `agent-learning.yml:218` fija
  `@anthropic-ai/claude-code@2.1.233`, posterior a la última versión corregida citada (2.1.163,
  2026-07-24) por 70 versiones de diferencia.
- **Segunda consulta, agregada**: se repitió la consulta pidiendo explícitamente el aviso con el rango
  vulnerable más alto; devolvió el mismo conjunto (el más reciente sigue siendo GHSA-7835-87q9-rgvv,
  corregido en 2.1.163) y ninguna entrada con un rango que alcance 2.1.233. Las dos consultas
  coinciden entre sí.
- **Límite del método**: ver nota al pie de «Fuentes consultadas» sobre el resumen por herramienta.
  Confianza alta en el rango de versiones citado (dos consultas independientes, coincidentes, contra la
  fuente primaria), sin ser lectura manual del JSON.
- **Confianza**: alta, con la salvedad de método anterior.

### H2 — La versión fijada del CLI (2.1.233) queda por detrás de `stable` (2.1.236) y bastante detrás de `latest`/`next` (2.1.252)

- **Estado**: hallazgo nuevo, del mismo tipo que H7 del informe anterior (deriva de versión fijada vs.
  entorno real), aplicado ahora a `@anthropic-ai/claude-code` en vez de a `npm`.
- **Verificado hoy**: `registry.npmjs.org/@anthropic-ai/claude-code` — `dist-tags`: `stable: 2.1.236`,
  `latest: 2.1.252`, `next: 2.1.252`. `agent-learning.yml:218` fija `2.1.233`.
- **Lectura**: no es una vulnerabilidad conocida (H1 ya estableció que 2.1.233 no está en ningún rango
  vulnerable publicado); es sólo distancia entre lo fijado y lo disponible. El propio archivo ya
  documenta por qué está fijado y no en `latest` (comentario junto a la línea 218: reproducibilidad de
  la corrida) y por qué el pin importa para otra cosa —el comentario de la línea 278 ya compara contra
  «claude-code 2.1.252» para justificar un acceso defensivo del parser de denegaciones, así que quien
  mantiene el workflow ya sabe que hay versiones más nuevas—. No se propone re-fijar automáticamente:
  igual que con `packageManager`, subir el pin es una decisión que alguien toma mirando el registro de
  cambios de las versiones intermedias, no un hallazgo que se resuelva solo.
- **Confianza**: alta en el hecho local y en los dist-tags.

### H3 — El backport de `actions/checkout` contra pwn requests queda corroborado con una segunda fuente primaria, y la fecha del informe anterior se corrige

- **Estado**: cierra la Pregunta abierta implícita del informe 2026-08-31 (sección «Fuentes externas»,
  párrafo final: «no se corroboró con una segunda fuente primaria»).
- **Verificado hoy**: `github.blog/changelog/2026-06-18-safer-pull_request_target-defaults-for-github-
  actions-checkout/` — nota editorial: el backport a las versiones mayores soportadas (todas salvo v1)
  se movió «from July 16, 2026 to Monday, July 20, 2026».
- **Corrección sobre el informe anterior**: la semana pasada se citó 2026-07-16 desde una fuente de
  prensa (The Hacker News), sin segunda fuente. La fecha correcta, según la propia nota editorial de
  GitHub, es 2026-07-20 — y coincide exactamente con la fecha de publicación de `actions/checkout
  v7.0.1` (verificado hoy: `api.github.com/repos/actions/checkout/releases/latest`, publicado
  2026-07-20T15:10Z), que es el SHA que los seis workflows de este repositorio ya tienen fijado. No
  cambia ninguna recomendación: el pin actual ya es posterior al backport bajo cualquiera de las dos
  fechas.
- **Confianza**: alta (dos fuentes primarias independientes, coincidentes en la fecha corregida).

### H4 — Los cuatro Actions fijados siguen siendo la última versión publicada de cada una

- **Estado**: corroboración de H4 del informe 2026-08-31, repetida con lectura directa de cada release
  en vez de inferencia por comentario.
- **Verificado hoy**: `actions/checkout` → último release `v7.0.1` (coincide con el SHA fijado);
  `actions/setup-node` → último release `v7.0.0` (coincide); `actions/download-artifact` → último
  release `v8.0.1` (coincide). No se repitió la consulta para `actions/upload-artifact` porque
  `dependabot.yml` agrupa las cuatro en una sola actualización mensual y las tres verificadas ya
  muestran que el mecanismo está al día.
- **Confianza**: alta.

### H5 — Node.js: sigue sin nueva security release; el piso de `engines.node` sigue abierto

- **Estado**: continúa abierto (recomendación #3 de este informe, arrastrada del #5 del informe
  2026-08-16).
- **Verificado hoy**: el índice de security releases de Node.js no lista ninguna entrada posterior al
  2026-07-29.
- **Sobre la Pregunta abierta 3 del informe anterior** («¿incluye 24.19.0 los tres CVE de julio?»): se
  abrió el changelog detallado de la entrada 24.19.0 (2026-08-03) y no menciona ninguno de los tres
  identificadores (`CVE-2026-56846`, `CVE-2026-56848`, `CVE-2026-58043`) por nombre. Esto **no**
  confirma que estén ausentes: los changelogs de una release regular de Node no necesariamente citan
  CVEs de fixes ya incorporados en una versión anterior de la misma línea, y no se comprobó ese patrón
  esta semana — afirmarlo sería una afirmación de mecanismo sin verificar (R14). La pregunta sigue
  abierta, ahora con este dato negativo agregado.
- **Confianza**: alta en «no hay release de seguridad nueva»; sin novedad en la hipótesis sobre
  24.19.0.

### H6 — npm v12 ya está publicado, no sólo anunciado; sigue vigente la recomendación de evaluarlo

- **Estado**: actualiza H7 del informe 2026-08-31 (que trataba a v12 como «fuente secundaria,
  pendiente»). Dato nuevo: ya no es un anuncio, es una versión publicada.
- **Verificado hoy**: `registry.npmjs.org/npm/latest` → `version: 12.0.2`. El release `v12.0.0` de
  `npm/cli` en GitHub está publicado el 2026-07-08 y describe los tres cambios de default que ya
  recogía como hipótesis el informe anterior: `allowScripts` pasa a bloquear scripts de ciclo de vida
  por defecto salvo permiso explícito, y `allow-git`/`allow-remote` pasan a `none` por defecto.
- **Local, sin cambios**: `package.json` sigue en `packageManager: npm@11.16.0`; el entorno de esta
  corrida sigue en npm 11.17.0 (dado por el job). Cauce no tiene dependencias de runtime ni scripts de
  ciclo de vida propios, así que el cambio de default no tiene efecto funcional conocido sobre este
  repositorio — es la misma lectura que la semana pasada, ahora con la versión confirmada en vez de
  «vendría en julio».
- **Confianza**: alta (versión publicada verificada en dos fuentes primarias: registro npm y release de
  GitHub, coincidentes).

### H7 — CISA KEV: sin entradas nuevas relevantes; ChainDrop: sin escalamiento adicional

- **Estado**: corroboración, sin hecho nuevo que cambie la lectura de los dos informes anteriores.
- **CISA KEV**: el fetch directo a la página del catálogo devolvió HTTP 403 esta semana (no la semana
  pasada); se corroboró por búsqueda que la última tanda de adiciones sigue siendo la del 2026-08-26
  (seis CVE: Cisco, Microsoft, VMware/etc., ninguno de Node.js/npm/GitHub Actions/JavaScript). No se
  encontró ninguna adición de septiembre 2026 al momento de esta corrida.
- **ChainDrop**: la búsqueda de esta semana no encontró una segunda ola nueva ni un paquete adicional
  relevante para el stack de Cauce; sólo detalle adicional sobre el mismo incidente del 2026-08-04 ya
  cubierto (mecanismo de C2 vía contrato de Ethereum, atribución a un grupo llamado TeamPCP en algunas
  fuentes de vendor research). Ninguno de esos detalles cambia la exposición de Cauce, que sigue sin
  dependencias de runtime (`package.json` sin bloque `dependencies`, sin cambios).
- **Confianza**: alta en «sin escalamiento nuevo relevante para Cauce»; el 403 de esta semana en el
  fetch directo de CISA es un hecho local de esta corrida, no una afirmación sobre el contenido del
  catálogo.

### H8 — La nueva entrada de `workflow_dispatch.inputs.cadence` no reabre la separación de privilegios que H3 (informe 2026-08-31) había cerrado

- **Estado**: revisión del único cambio de código de automatización desde el informe anterior; no se
  encontró un hallazgo nuevo, se registra la revisión porque toca el job con credencial de escritura.
- **Evidencia local**: `agent-learning.yml` — el nuevo input `cadence` y la variable `CADENCE` se leen
  con `process.env.CADENCE` dentro del script Node de `discover` (línea 89, 107-108); no se interpolan
  en un comando de shell. El resultado de ese script sólo decide qué slugs —ya validados contra
  `agents list --json`— entran en `agents`/`consolidate`; no cambia qué permisos tiene `research`
  (`contents: read`) ni `research-pr` (`contents: write`), ni el borde que separa uno del otro. Un
  `workflow_dispatch` con un valor de `cadence` que no matchea ninguna de las tres cadencias declaradas
  simplemente no filtra nada por cadencia (cae al branch `roles` sin filtrar), no ejecuta nada
  arbitrario.
- **Confianza**: alta (lectura directa del diff contra el informe anterior, sin ejecutar el workflow).

## Evidencia

Todo lo local fue verificado por lectura en esta máquina, sin tocar sistemas remotos, sin credenciales y
sin escanear nada. No se leyó ningún valor de `.env` (no existe en este árbol de trabajo). Las fuentes
externas se consultaron sólo por lectura (fetch/búsqueda), nunca ejecutando ni instalando nada.

| # | Evidencia | Tipo |
| --- | --- | --- |
| H1 | `api.github.com/advisories?affects=...` (dos consultas, 2026-09-01); 6 avisos individuales por GHSA id; `.github/workflows/agent-learning.yml:218` | externa primaria + local |
| H2 | `registry.npmjs.org/@anthropic-ai/claude-code` (dist-tags, 2026-09-01); `agent-learning.yml:218,278` | externa primaria + local |
| H3 | `github.blog/changelog/2026-06-18-...` (2026-09-01); `api.github.com/repos/actions/checkout/releases/latest` (2026-09-01) | externa primaria |
| H4 | releases `latest` de `actions/checkout`, `actions/setup-node`, `actions/download-artifact` (2026-09-01); `.github/workflows/*.yml` (SHAs fijados) | externa primaria + local |
| H5 | nodejs.org/en/blog/vulnerability/ (fetch 2026-09-01); CHANGELOG_V24.md, entrada 24.19.0 (fetch 2026-09-01) | externa primaria |
| H6 | `registry.npmjs.org/npm/latest` (2026-09-01); `github.com/npm/cli` release `v12.0.0` (2026-09-01); `package.json:44` | externa primaria + local |
| H7 | búsqueda sobre catálogo CISA KEV y sobre ChainDrop (2026-09-01); `package.json` sin bloque `dependencies` | externa (agregada por búsqueda, fetch directo de CISA falló con 403) + local |
| H8 | `.github/workflows/agent-learning.yml` (diff contra la versión leída el 2026-08-31): líneas 12-40, 85-140 | local |

## Posibles prácticas obsoletas

1. **`@anthropic-ai/claude-code` fijado tres versiones por detrás de `stable` y diecinueve por detrás de
   `latest`/`next`.** Ver H2. No es una vulnerabilidad conocida (H1), es distancia entre lo fijado y lo
   disponible — el mismo patrón que ya está señalado para `packageManager` (H7 del informe anterior).
   (Confianza: **alta**.)
2. **`.env.example` sigue recomendando un token npm «clásico» que ya no se puede crear.** Sin cambios
   desde el informe anterior (H2 de 2026-08-31); no se volvió a verificar la fecha de revocación esta
   semana porque nada local cambió. (Confianza: **sin novedad, carried forward**.)
3. **`engines.node: ">=24"` sin piso de parche.** Sin cambios; ver H5. (Confianza: **alta**.)
4. **`packageManager: npm@11.16.0`, y ahora con npm v12 ya publicado (no sólo anunciado).** Ver H6.
   (Confianza: **alta** en el hecho local y en la publicación de v12; sin impacto funcional conocido
   sobre Cauce.)

## Recomendación

Nada de lo que sigue se aplicó: este informe no toca dependencias, controles, secretos, código,
`SKILL.md` ni planificación.

**Prioridad media**

1. Subir el pin de `@anthropic-ai/claude-code` en `agent-learning.yml` de `2.1.233` a una versión más
   cercana a `stable` (2.1.236) o `latest` (2.1.252) en algún momento, revisando el registro de cambios
   intermedio antes de mover el número — no porque 2.1.233 esté afectado por un aviso conocido (no lo
   está, H1), sino porque quedarse muy por detrás de `latest` es la misma exposición que H2 describe:
   un aviso que se publique mañana puede alcanzar versiones más nuevas que la fijada sin que nadie lo
   note hasta la próxima corrida de este mismo informe. Cierra H2.
2. Evaluar mover `packageManager` a npm v12, ahora que está publicado y no sólo anunciado (sin impacto
   funcional conocido: Cauce sigue sin dependencias de runtime ni scripts de ciclo de vida). Actualiza
   la recomendación #4 del informe 2026-08-31 (H7 de esa fecha, H6 de éste).

**Prioridad baja — arrastradas de informes anteriores, sin cambios**

3. Subir el piso de `engines.node` a la línea parcheada vigente en vez de `>=24` abierto (H5).
4. Actualizar (o retirar) la guía de `NPM_TOKEN` en `.env.example`, que sigue nombrando un token
   «clásico» que npm no permite crear (recomendación #1 del informe 2026-08-31, sin decisión humana
   registrada todavía — ver Preguntas abiertas).

**Sobre el contrato del cargo**: nada de esto exige cambiar `SKILL.md` ni
`evaluations/expected-behaviors.yaml`. El único cambio de código de automatización desde el informe
anterior (`workflow_dispatch.inputs.cadence` en `agent-learning.yml`) se revisó contra la separación de
privilegios que H3 del informe 2026-08-31 había cerrado y no la reabre (H8) — no genera una
recomendación nueva.

## Preguntas abiertas

1. **¿Sigue siendo necesaria la vía de publicación manual (`npm publish` + `NPM_TOKEN`)?** Arrastrada
   del informe 2026-08-31 (H2, recomendación #2 de esa fecha); requiere una respuesta de quien mantiene
   el paquete, no una inferencia de este cargo.
2. **¿Por qué salió `.github/workflows/` del `files` de `package.json`?** Arrastrada del informe
   2026-08-16; el historial de git disponible en esta corrida sigue siendo un solo commit y no alcanza
   para atribuir intención.
3. **¿Incluye Node.js v24.19.0 los tres CVE High corregidos en v24.18.1 (2026-07-29)?** El changelog de
   24.19.0 no cita esos tres identificadores por nombre, pero eso no confirma su ausencia — no se
   estableció esta semana si los changelogs de Node citan CVEs de fixes ya incorporados en versiones
   anteriores de la misma línea. Sigue sin poder confirmarse sin una fuente que lo diga explícitamente.
   No sostiene ninguna recomendación de este informe.
4. **Estado de SSDF v1.2.** Seguía en comentario público hace dos semanas; no se volvió a comprobar
   esta semana tampoco. Requiere abrir `csrc.nist.gov` de nuevo.
5. **¿El listado de la API de GitHub Advisory Database para `@anthropic-ai/claude-code` es exhaustivo?**
   Este informe lo trató como tal porque dos consultas independientes coincidieron entre sí, pero
   ambas pasaron por el resumen de un modelo sobre el JSON crudo (ver nota en «Fuentes consultadas»),
   no por una lectura manual de cada entrada del arreglo. Si en algún momento se necesita una garantía
   más fuerte que «alta confianza», hace falta leer el JSON completo sin intermediarios o correr
   `npm audit` sobre una instalación real de la herramienta — ninguna de las dos se hizo acá.
