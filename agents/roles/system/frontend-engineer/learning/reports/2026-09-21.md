---
agent: frontend-engineer
date: 2026-09-21
status: draft
propone: no
---

# Investigación semanal — 2026-09-21

<!-- Tres convenciones que el ciclo necesita y que nada más sostiene:

  · Etiquetá cada hallazgo H1, H2, … en el orden en que aparecen. «Evidencia» y «Recomendación» se
    refieren a ellos por esa clave, y la propuesta mensual la cita para decir de qué hallazgo sale
    un cambio. Sin etiqueta las tres secciones dejan de cruzarse y nada lo delata.

  · No renombres los títulos. «## Recomendación» se lee con un patrón exacto y es lo único que la
    propuesta consolida de cada informe: renombrarlo no da error, deja la propuesta vacía.

  · Contestá «propone» en el frontmatter, con «si» o con «no». Es «si» cuando el informe pide tocar
    algún archivo del cargo —SKILL.md, sources.yaml, references/, un caso— y «no» cuando lo hallado
    no cambia ningún contrato, aunque «Recomendación» explique largamente por qué. Un «no» se
    mergea sin revisión humana: no hay nada que decidir. Ante la duda va «si», que sólo cuesta una
    mirada.

Este comentario vive fuera de toda sección a propósito — dentro de «Recomendación» viajaría a cada
propuesta consolidada. -->

## Fuentes consultadas

Igual que en los tres informes anteriores (2026-08-31, 2026-09-07, 2026-09-14): este repositorio es el
toolkit (`mode: toolkit`), no una instancia, así que no existe `organization/roles/frontend-engineer.md`
ni manifiesto de proyecto (`package.json` con stack frontend) contra el cual fijar versiones soportadas —
el único `package.json` de este repo es el del propio paquete `@ingeniomaps/cauce` (CLI Node, sin stack
frontend). El entorno de esta corrida es Node v24.20.0 y npm 11.19.0, medido por el job que la lanzó, no
por esta sesión (no se volvió a comprobar); ningún hallazgo de esta corrida depende de esa versión.

Las cuatro fuentes de `learning/sources.yaml` se consultaron por WebFetch/WebSearch directo, acotando la
ventana a 2026-09-14 – 2026-09-21 salvo donde se dice lo contrario:

- MDN Web Docs — `developer.mozilla.org/en-US/blog/` (listado completo, sin entradas nuevas en la
  ventana).
- W3C WAI — `w3.org/WAI/news/` (listado completo, sin ítems nuevos en la ventana).
- OWASP Cheat Sheet Series — `api.github.com/repos/OWASP/CheatSheetSeries/commits` acotado con
  `since`/`until` a la ventana, para los trece commits que devolvió; `Index.html` del sitio publicado y
  `contents/cheatsheets` de la API para ubicar el cheat sheet nuevo; su página publicada
  (`cheatsheetseries.owasp.org/cheatsheets/AI-Powered_Advertising_Systems_Security_Cheat_Sheet.html`) y el
  PR que lo agregó (`github.com/OWASP/CheatSheetSeries/pull/2380`).
- web.dev — `web.dev/blog/baseline-digest-aug-2026` (404), `web.dev/baseline/2026`,
  `web.dev/blog/baseline-digest-jul-2026` (releída buscando enlace a una edición posterior),
  `web.dev/series/new-to-the-web`, búsquedas web directas "web.dev baseline digest August 2026" y
  "web.dev New to the web platform August 2026".

Se agregaron fuentes fuera del catálogo, todas para cerrar preguntas abiertas de informes anteriores, no
para hallazgos nuevos:

- Bugzilla (Mozilla) — `bugzilla.mozilla.org/show_bug.cgi?id=2030645`, para la versión exacta de Firefox
  de la pregunta abierta sobre container style queries.
- W3C — `lists.w3.org/Archives/Public/w3c-wai-gl/2026JulSep/0129.html` (releída) y una búsqueda web
  dirigida a esa misma lista, para el resultado del Call For Consensus del 2 de septiembre.
- Chrome for Developers — `developer.chrome.com/docs/web-platform/deprecating-xslt` (releída, verificación
  de continuidad sobre H1 de 2026-09-07 y H1 de 2026-09-14).

## Hallazgos

- **H1 — MDN Web Docs, sin entradas de blog en la ventana 2026-09-14 a 2026-09-21**. **Verificado** por
  lectura directa de `developer.mozilla.org/en-US/blog/`: la entrada más reciente sigue siendo la del 15
  de junio de 2026 ("Introducing the MDN MCP server"), igual que en el informe anterior. No es evidencia
  de que MDN no haya cambiado nada esta semana —su documentación de referencia se edita fuera del
  blog—, sólo de que no hubo anuncio.
- **H2 — W3C WAI, sin ítems nuevos desde el 2026-09-10**. **Verificado** por lectura directa de
  `w3.org/WAI/news/`: el ítem más reciente sigue siendo "For Review: WCAG 3 Working Draft – September
  2026" del 2026-09-10, ya reportado como H2 del informe anterior. El resultado formal del Call For
  Consensus del 2 de septiembre (pregunta abierta desde el informe 2026-09-07) sigue sin encontrarse: la
  lista de correo pública muestra votos individuales "+1" (Roberto Scano, Léonie Watson) fechados
  2026-09-02, pero ningún mensaje de cierre que declare el resultado del CFC ni que lo vincule con la
  publicación del borrador del 10 de septiembre. La correlación de fechas —el plazo vencía el 7 de
  septiembre y el borrador salió tres días después— sigue siendo sugestiva y no confirmada, igual que en
  el informe anterior.
- **H3 — OWASP Cheat Sheet Series, sin cambios en los tres archivos rastreados y un cheat sheet nuevo de
  encaje débil**. Se revisó el historial completo de commits del repositorio en la ventana 2026-09-14 a
  2026-09-21 (trece commits, vía la API de GitHub con `since`/`until`); ninguno toca
  `Content_Security_Policy_Cheat_Sheet.md`, `Cross_Site_Scripting_Prevention_Cheat_Sheet.md` ni
  `Secure_Coding_with_AI_Cheat_Sheet.md`. **Verificado** por lectura directa de la lista de commits. Sí se
  agregó un cheat sheet nuevo al catálogo del proyecto — "AI-Powered Advertising Systems Security"
  (PR #2380, mergeado 2026-09-14) —, cuyo alcance es seguridad de modelos de IA en sistemas de
  publicidad (envenenamiento de datos de entrenamiento, inyección de prompt, extracción de modelo,
  cadena de suministro), no vulnerabilidades de aplicación web tradicionales. **Verificado** por lectura
  directa de la página publicada: trae una sola mención de comportamiento de navegador —que los modelos
  on-device del Chrome Prompt API sólo alcanzan la ventana de nivel superior y sus iframes del mismo
  origen, que un iframe de anuncio de origen cruzado nunca debería recibir el token de delegación
  `allow="language-model"`, y que CSP no se hereda hacia ese tipo de iframe—, pero el resto del documento
  no encaja en ninguno de los `topics` que `sources.yaml` declara para OWASP hoy (`xss, csp,
  browser-security, session`). Mismo patrón que H8 del informe 2026-09-07 con "Secure Coding with AI":
  fuente del catálogo, tema fuera de los `topics` declarados.
- **H4 — web.dev, digest de agosto 2026 sigue sin encontrarse** (misma pregunta abierta que los dos
  informes anteriores, ahora en su tercera semana). Esta semana se agregaron tres intentos más:
  `web.dev/blog/baseline-digest-aug-2026` devuelve HTTP 404; `web.dev/baseline/2026` no lista fechas de
  agosto ni septiembre; y una búsqueda dirigida a la serie hermana "New to the web platform" tampoco
  devolvió una entrada de agosto 2026 (la más reciente que aparece en resultados de búsqueda es la de
  junio). Sigue sin ser una confirmación de ausencia — la página de la serie en sí
  (`web.dev/series/new-to-the-web`) no pudo leerse como listado porque su contenido depende de
  renderizado dinámico que WebFetch no ejecuta, la misma limitación que ya afectaba a los intentos
  anteriores sobre `web.dev/blog`.
- **H5 — Container style queries, versión exacta de Firefox para soporte completo** (cierra la pregunta
  abierta de H3 del informe 2026-09-14, que a su vez venía de H4 de los informes 2026-09-07 y
  2026-08-31). **Verificado**: `bugzilla.mozilla.org/show_bug.cgi?id=2030645` ("Enable container style
  queries on all channels"), campo "Target Milestone" en "151 Branch", estado "RESOLVED FIXED", con un
  comentario que confirma "Added to the final Fx151 release notes". Firefox 151 es la versión exacta en
  la que el soporte completo se activó por defecto en todos los canales, no sólo la más vieja que
  caniuse.com todavía lista (que es lo único que el informe anterior había podido establecer).
- **H6 — XSLT, sin cambios en la página de Chrome for Developers** (verificación de continuidad sobre H1
  de 2026-09-07 e informes siguientes). **Verificado**: `developer.chrome.com/docs/web-platform/deprecating-xslt`
  sigue con el mismo cronograma —remoción en Stable el 17 de noviembre de 2026, Chrome 158— y su marca de
  última actualización sigue en 2025-10-29, sin cambios desde entonces. No es un hallazgo nuevo, es la
  comprobación de que el hallazgo ya registrado sigue vigente.

## Evidencia

- H1: `developer.mozilla.org/en-US/blog/`, leída 2026-09-21, última entrada 2026-06-15.
- H2: `w3.org/WAI/news/`, leída 2026-09-21, sin ítem posterior al 2026-09-10;
  `lists.w3.org/Archives/Public/w3c-wai-gl/2026JulSep/0129.html`, votos "+1" fechados 2026-09-02, sin
  mensaje de cierre del CFC encontrado.
- H3: `api.github.com/repos/OWASP/CheatSheetSeries/commits?since=2026-09-14T00:00:00Z&until=2026-09-21T23:59:59Z`
  (trece commits, ninguno sobre los tres archivos rastreados);
  `github.com/OWASP/CheatSheetSeries/pull/2380` (cheat sheet nuevo, mergeado 2026-09-14);
  `cheatsheetseries.owasp.org/cheatsheets/AI-Powered_Advertising_Systems_Security_Cheat_Sheet.html`
  (contenido y la mención de CSP/iframe).
- H4: `web.dev/blog/baseline-digest-aug-2026` (HTTP 404); `web.dev/baseline/2026` (sin fechas de
  agosto/septiembre); búsqueda web "web.dev New to the web platform August 2026" (sin resultado de
  agosto 2026).
- H5: `bugzilla.mozilla.org/show_bug.cgi?id=2030645`, "Target Milestone: 151 Branch", "RESOLVED FIXED",
  comentario "Added to the final Fx151 release notes".
- H6: `developer.chrome.com/docs/web-platform/deprecating-xslt`, leída 2026-09-21, cronograma sin cambios,
  última actualización 2025-10-29.

## Posibles prácticas obsoletas

- Transformar XML del lado del cliente con XSLT (arrastra H1 de 2026-09-07 y su seguimiento en los
  informes siguientes): sin cambios esta semana (H6). Sigue **verificado** para Chrome, Edge, WebKit y
  Gecko (esto último desde el informe 2026-09-14), con fecha dura del 17 de noviembre de 2026 para dejar
  de funcionar en Chrome Stable.

## Recomendación

Ninguna de estas se aplica en este ciclo — el rol prohíbe reescribirse durante el aprendizaje y el cambio
de SKILL.md sólo puede salir de una propuesta mensual evaluada y aprobada. Esta semana no hay hallazgo
nuevo que agregue una recomendación distinta de las ya registradas en `2026-09-r2.md` (archivada sin
decidir el 2026-09-14) y en el informe de 2026-09-07: siguen siendo las mismas, y lo único que cambió es
que H5 de este informe cierra uno de los datos que las sostenía.

- El riesgo de descontinuación de XSLT en cliente sigue como criterio de revisión de código existente, no
  como cambio de SKILL.md — sin novedad esta semana (H6).
- Mencionar `field-sizing` (CSS) en SKILL.md § "Reglas de construcción" como alternativa nativa a expandir
  `<textarea>` con JavaScript sigue pendiente de una propuesta mensual, sin cambios esta semana.
- La pregunta de alcance sobre si "Secure Coding with AI" entra en `sources.yaml` sigue sin resolverse
  (H8 del informe 2026-09-07). El cheat sheet nuevo de este informe (H3, "AI-Powered Advertising Systems
  Security") plantea la misma pregunta de alcance con la misma respuesta tentativa: no se recomienda
  agregarlo — su tema no encaja en los `topics` declarados para OWASP y su superposición con el trabajo
  del cargo es marginal (una mención de CSP/iframe dentro de un documento sobre seguridad de modelos de
  IA en publicidad). No se propone tocar `sources.yaml` por esto.

## Preguntas abiertas

- H2 (CFC de WCAG 3 del 2 de septiembre): sigue sin encontrarse un mensaje que declare el resultado formal
  del Call For Consensus ni que lo vincule con el borrador del 10 de septiembre. Esta semana se agregó
  evidencia parcial —votos "+1" individuales fechados 2026-09-02— pero no un cierre. Falta revisar el
  archivo completo de la lista `w3c-wai-gl` para el hilo específico del CFC, algo que las dos búsquedas de
  esta semana no cubrieron.
- H3 (cheat sheet "AI-Powered Advertising Systems Security"): la decisión de no recomendarlo para
  `sources.yaml` es tentativa, igual que la de "Secure Coding with AI" en el informe 2026-09-07 — las dos
  quedan como la misma pregunta de alcance sin resolver, no como dos preguntas distintas.
- H4 (digest de agosto 2026 de web.dev): sigue sin encontrarse, ahora tras tres semanas y varios intentos
  por URL directa, listado de `/blog`, búsqueda web y la serie hermana "New to the web platform". Ninguno
  confirma que no exista, sólo que no se lo encontró por esas vías — y el listado dinámico de
  `web.dev/blog` y de `web.dev/series/new-to-the-web` sigue sin poder leerse con WebFetch, una limitación
  de herramienta y no de la fuente.
- La pregunta de alcance de `sources.yaml` sobre "Secure Coding with AI" (H8 del informe 2026-09-07) sigue
  abierta: es una decisión de catálogo, no un hecho verificable, y no le toca a este informe resolverla.
- Este repositorio sigue sin manifiesto de proyecto frontend propio (es el toolkit, no una instancia), así
  que ningún hallazgo de esta corrida pudo contrastarse contra "versiones soportadas" reales de un stack —
  quedan generales a la plataforma web, igual que en los tres informes anteriores.
