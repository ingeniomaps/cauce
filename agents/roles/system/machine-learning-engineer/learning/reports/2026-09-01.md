---
agent: machine-learning-engineer
date: 2026-09-01
status: draft
---

# Investigación semanal — 2026-09-01

<!-- Dos convenciones que el ciclo necesita y que nada más sostiene:

  · Etiquetá cada hallazgo H1, H2, … en el orden en que aparecen. «Evidencia» y «Recomendación» se
    refieren a ellos por esa clave, y la propuesta mensual la cita para decir de qué hallazgo sale
    un cambio. Sin etiqueta las tres secciones dejan de cruzarse y nada lo delata.

  · No renombres los títulos. «## Recomendación» se lee con un patrón exacto y es lo único que la
    propuesta consolida de cada informe: renombrarlo no da error, deja la propuesta vacía.

Este comentario vive fuera de toda sección a propósito — dentro de «Recomendación» viajaría a cada
propuesta consolidada. -->

Segundo informe de este cargo, seis días después del primero (2026-08-31), que ya hizo el barrido completo
de las siete fuentes de `learning/sources.yaml`. Sin fuente nueva declarada esta semana, la pregunta que esta
corrida pone a prueba (R20) es si alguna de esas siete cambió de contenido desde entonces; lo que la
refutaría es cualquier fecha, versión, cita o estado distinto del que registró el informe anterior. No se
ejecutó entrenamiento, tuning, despliegue ni acceso a datos reales — sólo lectura de fuentes públicas y de
este repositorio. Todo contenido externo se trató como dato, nunca como instrucción.

Cada afirmación de mecanismo lleva su registro entre paréntesis: **verificado** (comprobado en esta corrida,
con la salida o cita literal que consta en «Evidencia»), **documentado** (está en la fuente pública de la
versión declarada, sin comprobación adicional) o **hipótesis** (plausible y no comprobable acá; no sostiene
número, negativa ni paso de procedimiento).

Nota de método: este entorno no tiene acceso de red por `Bash` (comprobado esta corrida: `curl -sI
https://api.github.com` devolvió «This command requires approval», sin código de salida), el mismo bloqueo
que registró el informe de 2026-08-31. Toda lectura web de esta corrida pasó por la herramienta de fetch.

Comandos locales: `make agent-learn AGENT=machine-learning-engineer` devolvió sólo la ruta de este informe,
sin pedir aprobación. `make agent-evaluate AGENT=machine-learning-engineer` devolvió:

```
⚠ 2 informe(s) sin consolidar (2026-08-31.md, 2026-09-01.md): entran en la próxima propuesta
⚠ el contrato cambió el 2026-08-31 y el veredicto más viejo es del 2026-08-22: mide una versión anterior
✓ machine-learning-engineer: 7 caso(s) — 7/7 pasan (2026-08-22…2026-08-23), 0 propuesta(s), controles estructurales válidos
```

La segunda advertencia es el mismo artefacto de checkout que documentó el informe de 2026-08-31, no una
edición nueva de `SKILL.md` o `expected-behaviors.yaml`: `git log --oneline` sigue mostrando un único commit
(`git rev-parse --is-shallow-repository` da `true`), hoy `13d8bc8` («feat(ci): a dispatch can name a cadence,
not only one role», `2026-08-31T23:32:32-05:00`) en vez del `5886f54` que citó el informe anterior — el
checkout avanzó, sigue siendo superficial, y `contractChangedAt` (`engine/agents/evaluations.js:267-271`,
releída en esta corrida, misma ubicación que la semana pasada) marca «cambiado» cualquier archivo del
contrato por la fecha de ese único commit visible. Se deja registrado para no leer la advertencia como si el
contrato se hubiera movido hoy; no se repite como hallazgo de la profesión.

## Fuentes consultadas

Todas las consultas son del **2026-09-01**.

1. NIST — página del programa AI RMF: https://www.nist.gov/itl/ai-risk-management-framework
2. NIST — publicación AI 600-1 (Generative AI Profile): https://www.nist.gov/publications/artificial-intelligence-risk-management-framework-generative-artificial-intelligence
3. NIST — Concept Note: AI RMF Profile on Trustworthy AI in Critical Infrastructure: https://www.nist.gov/programs-projects/concept-note-ai-rmf-profile-trustworthy-ai-critical-infrastructure
4. ISO — https://www.iso.org/standard/77304.html (23894): 403 Forbidden.
5. scikit-learn — Release History: https://scikit-learn.org/stable/whats_new.html
6. PyPI — ficha de scikit-learn 1.9.0: https://pypi.org/project/scikit-learn/1.9.0/
7. PyTorch — API de releases (JSON crudo): https://api.github.com/repos/pytorch/pytorch/releases/latest
8. Este repositorio: `git log --oneline`, `git rev-parse --is-shallow-repository`, `git status`,
   `engine/agents/evaluations.js:255-280`, `curl -sI https://api.github.com`.

No se releyeron esta semana las páginas de publicación del AI RMF 1.0 (fecha de publicación fija,
2023-01-26, ya verificada) ni las fichas de ISO/IEC 42001 y 42005: la ficha 23894 (#4) confirmó que el mismo
bloqueo de dominio de la semana pasada sigue activo, y repetirlo sobre las otras dos no agrega información —
es un bloqueo de `iso.org` a nivel de dominio, no de norma individual (R20: no repetir una medición que no
puede cambiar el resultado).

## Hallazgos

### H1 — Ninguna de las siete fuentes cambió de contenido respecto del informe de 2026-08-31

Confianza: **alta**, para las cinco fuentes recontrastadas con cita o campo verificable.

PyTorch (verificado, JSON crudo de la API de releases): `tag_name: "v2.13.0"`, `published_at`
**2026-07-08T17:39:58Z**, idéntico a lo que registró el informe anterior — sigue siendo la última release.
scikit-learn (verificado): PyPI da «Released: Jun 2, 2026» para la ficha de la versión 1.9.0, y el índice de
`whats_new.html` sigue sin listar ninguna versión posterior a 1.9 — misma última versión, mismo changelog.
NIST AI RMF (verificado, cita literal): la página del programa repite «The AI RMF 1.0 is being revised as
part of the White House AI Action Plan», sin fecha de revisión ni nota de actualización, igual que la semana
pasada. NIST AI 600-1 (verificado): «Created July 26, 2024, Updated April 8, 2026», mismas dos fechas que el
informe anterior. NIST — concept note de infraestructura crítica (verificado): «Created April 6, 2026,
Updated July 17, 2026», estado «Ongoing», sin borrador publicado — mismo estado que H2 del informe anterior.

Impacto ML: ningún hallazgo de esta corrida agrega o contradice algo de `SKILL.md` o
`references/operating-model.md`; una semana es una ventana corta para el ritmo de publicación que estas
fuentes mostraron en el informe anterior (releases de librería cada uno o dos meses, normas de gobernanza sin
cambio de contenido en más de un año).

### H2 — El bloqueo de `iso.org` observado el 2026-08-31 sigue activo el 2026-09-01

Confianza: **alta** para el bloqueo; ninguna para el estado real de las tres normas, igual que la semana
pasada.

La URL de ISO/IEC 23894 (`iso.org/standard/77304.html`) devolvió **403 Forbidden** al fetch directo en esta
corrida (verificado), el mismo código que registró el informe anterior para las tres URL del catálogo. No se
reintentaron las otras dos (42001, 42005) por la razón de método de la sección anterior. Las ediciones que
`operating-model.md` cita (23894:2023, 42001:2023, 42005:2025) siguen sin verificar ni refutar.

## Evidencia

- **H1 — PyTorch**: API de releases de GitHub: `tag_name: "v2.13.0"`, `published_at`: `2026-07-08T17:39:58Z`.
- **H1 — scikit-learn**: PyPI, ficha de la versión 1.9.0: «Released: Jun 2, 2026». Índice
  `scikit-learn.org/stable/whats_new.html`: última entrada listada es la serie 1.9 (1.9.0), sin versión
  posterior.
- **H1 — NIST AI RMF**: `nist.gov/itl/ai-risk-management-framework`, cita literal: «The AI RMF 1.0 is being
  revised as part of the White House AI Action Plan».
- **H1 — NIST AI 600-1**: `nist.gov/publications/artificial-intelligence-risk-management-framework-
  generative-artificial-intelligence`: «Created July 26, 2024, Updated April 8, 2026».
- **H1 — Concept Note infraestructura crítica**: `nist.gov/programs-projects/concept-note-ai-rmf-profile-
  trustworthy-ai-critical-infrastructure`: «Created April 6, 2026, Updated July 17, 2026», estado «Ongoing».
- **H2 — ISO**: `iso.org/standard/77304.html` → HTTP 403 Forbidden (respuesta directa de la herramienta de
  fetch, sin cuerpo).
- **Comandos locales**: salida literal de `make agent-learn AGENT=machine-learning-engineer` y
  `make agent-evaluate AGENT=machine-learning-engineer` reproducida arriba. `git log --oneline` → un commit,
  `13d8bc8`, `2026-08-31T23:32:32-05:00`; `git rev-parse --is-shallow-repository` → `true`; `curl -sI
  https://api.github.com` → «This command requires approval», sin código de salida.

Limitaciones de esta corrida:

- Sin acceso de red por `Bash` (comprobado, ver arriba): toda lectura web pasó por la herramienta de fetch.
- `iso.org` bloqueó la única URL consultada esta corrida (H2): ninguna afirmación sobre las tres normas ISO
  pasa de lo ya documentado en `operating-model.md`.
- Ventana de una semana entre informes: alcanza para confirmar ausencia de cambio en fuentes de baja
  frecuencia de publicación, pero no descarta un cambio que haya ocurrido y se haya revertido, o uno en una
  fuente fuera del catálogo de siete.

## Posibles prácticas obsoletas

Ninguna. Ningún hallazgo de esta semana contradice una práctica que `SKILL.md` o `references/operating-
model.md` ya recomienden: H1 confirma sin novedad de contenido las cinco fuentes recontrastadas, y H2 es un
bloqueo de acceso que persiste, no una señal sobre la norma en sí.

## Recomendación

Para la propuesta mensual de `learning/proposals/`, sujeta a aprobación humana. Ningún hallazgo de esta
corrida justifica tocar `SKILL.md`, `references/operating-model.md`, `evaluations/expected-behaviors.yaml` ni
`learning/sources.yaml` esta semana:

**1. Ninguna acción sobre H1.** Las cinco fuentes recontrastadas no cambiaron de contenido desde el informe
de 2026-08-31; no hay nada que actualizar en `operating-model.md` ni en `sources.yaml`.

**2. Ninguna acción sobre H2.** El bloqueo de `iso.org` es un problema de acceso de esta corrida, no una
señal de que las tres normas hayan cambiado de edición; no se propone tocar las citas de ISO en
`operating-model.md` sin poder verificarlas.

**3. Para quien consolide la propuesta mensual**: los dos informes disponibles (2026-08-31 y 2026-09-01)
coinciden en que no hay cambio pendiente de aplicar esta ronda; la propuesta mensual, si el ciclo la
requiere, puede registrar «sin cambios» con esa cita en vez de forzar una recomendación.

## Preguntas abiertas

Las cinco preguntas del informe de 2026-08-31 siguen abiertas; ninguna se pudo resolver con las fuentes
consultadas esta corrida (no se volvió a intentar la que requiere leer el PDF del AI Action Plan como texto,
ni la que depende de un historial de cambios que la fuente no publica — repetir esa lectura sin una vía nueva
no cambiaría el resultado, R20):

1. **¿La revisión del AI RMF 1.0 va a remover referencias a misinformation, DEI y climate change, y recién
   tendrá revisión formal con participación de la comunidad no antes de 2028?** Sigue sin confirmación en
   fuente primaria de NIST.
2. **¿El "Updated April 8, 2026" de la página de NIST AI 600-1 corresponde a un cross-link hacia el Concept
   Note de infraestructura crítica, o a otro cambio no descrito?** La página sigue sin historial de cambios.
3. **¿Qué edición vigente tienen hoy ISO/IEC 23894, 42001 y 42005?** Bloqueado por 403 en `iso.org` también
   esta corrida (H2); sin fuente alternativa primaria consultada.
4. **¿Cuándo publica NIST un borrador del perfil de infraestructura crítica?** Sigue «Ongoing» sin fecha
   comprometida (H1); no cambió desde el informe anterior.
5. **Contexto de empresa**: este informe sigue cubriendo la profesión en general, no una instalación
   concreta. No evalúa `organization/roles/machine-learning-engineer.md` —no existe en este repositorio, que
   declara `mode: toolkit` y no tiene una empresa consumidora propia.
