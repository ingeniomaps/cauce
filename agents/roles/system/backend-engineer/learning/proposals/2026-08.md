---
agent: backend-engineer
period: 2026-08
status: proposed
automatic_apply: false
---

# Propuesta mensual — 2026-08

## Hallazgos

### 2026-08-27

Fuente interna: `agents/roles/system/backend-engineer/learning/reports/2026-08-27.md`

Ninguno de los hallazgos exige tocar `SKILL.md`, `references/operating-model.md` ni
`evaluations/expected-behaviors.yaml` esta semana: son cambios reales y verificados, pero encajan dentro
de las reglas de construcción ya vigentes («usar semántica HTTP coherente», «diseñar operaciones
repetibles o claves de idempotencia», «excluir credenciales y datos personales» en observabilidad) sin
contradecirlas. Dejo consignados como candidatos para una propuesta mensual, a decidir por quien
consolide, y no los aplico yo: agregar `RFC 10008` y `OpenAPI 3.2.0` al fundamento externo de
`operating-model.md` cuando haya evidencia de adopción real en un proyecto (H1, H2); y, si
`learning/sources.yaml` alguna vez amplía su alcance más allá de API-Security, distinguir explícitamente
el proyecto OWASP Top 10 general del proyecto OWASP API Security para no mezclar sus numeraciones (H4).

## Evidencia

`learning/reports/2026-08-27.md`, con sus fuentes primarias enlazadas. Los hallazgos son reales y están
verificados; lo que este documento registra es que ninguno pide tocar un contrato.

## Cambio propuesto

**Ninguno.** El período se revisó y no hay nada que cambiar: `SKILL.md`,
`references/operating-model.md` y `evaluations/expected-behaviors.yaml` quedan como están.

Lo que el informe encontró encaja dentro de reglas de construcción ya vigentes —«usar semántica HTTP
coherente», «diseñar operaciones repetibles o claves de idempotencia», «excluir credenciales y datos
personales» en observabilidad— sin contradecirlas, así que agregarlas repetiría lo que el contrato ya
dice. Dos quedan como candidatas para un período futuro, cada una con la condición que las activa:

- Sumar `RFC 10008` y `OpenAPI 3.2.0` al fundamento externo de `references/operating-model.md`, **cuando
  haya evidencia de adopción real en un proyecto** (H1, H2).
- Distinguir el proyecto OWASP Top 10 general del OWASP API Security para no mezclar sus numeraciones,
  **si `learning/sources.yaml` amplía su alcance más allá de API-Security** (H4).

Firmar esto autoriza la decisión de no cambiar nada, que es una decisión y no un trámite: deja el
período cerrado y las dos candidatas registradas con su condición, en vez de perderlas.

## Riesgos y regresiones

Ninguna. No se toca ningún contrato, así que ningún caso puede cambiar de veredicto.

## Evaluación

No corresponde volver a correr los casos: el contrato queda idéntico y una re-corrida mediría varianza,
no un cambio —lo que R20 nombra como comprar un número nuevo con la misma información—. Los veredictos
vigentes siguen valiendo.

## Aprobación humana

- Estado: pendiente
- Responsable: por definir
- Fecha: por definir
